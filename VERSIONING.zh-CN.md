# 版本管理规则

本项目同时包含硬件、固件、协议、持久化数据和主机工具，必须分层标识版本。一个文件名中的 `v1` 不代表整个仓库的发行版。

## 五类版本

1. **项目发布版本**：Git tag 使用语义化版本 `vMAJOR.MINOR.PATCH`，这是对外引用整个仓库快照的唯一版本。当前尚未打第一个 tag。
2. **正式固件入口名**：`mini_synth_v1` 是当前产品/应用系列名，为兼容现有脚本暂不随每次发布改名。实际构建必须同时记录 Git commit、完整 FQBN 和二进制 SHA-256。
3. **线协议/文件格式**：MUSB v1、MSPKG v1 独立演进。只有不兼容的帧或文件格式变化才升级它们，不能因普通 UI 功能变化升级。
4. **内部持久化 schema**：Settings v2 只表示 NVS 记录布局。它必须向后迁移或明确回退，不能当项目版本。
5. **硬件版本**：当前公开工程还没有明确丝印 revision，因此暂以设计源文件 SHA-256、日期和对应 Git commit 标识。下一次改板应在原理图、PCB 丝印和文档中统一加入如 `HW REV B`，并保留旧版硬件说明。

## 分支与提交

- `main` 只保存能够编译、主机测试通过且文档状态一致的集成点。
- 新功能使用短生命周期分支，例如 `codex/loop-sequencer` 或 `feature/loop-sequencer`；一次只承担一个主要功能或硬件变量。
- 推荐提交前缀：`feat(firmware)`、`feat(tools)`、`fix(...)`、`docs`、`test`、`chore(repo)`、`hardware`。
- 功能代码、硬件设计、生成产物和大规模文档整理尽量分提交。提交信息写清“结果”，正文补充安全边界或迁移影响。
- 不提交 `build/`、`logs/`、备份、个人路径、COM 号、歌曲包、Python 缓存、编辑器临时目录或第三方库副本。

## 发布规则

- `PATCH`：兼容修复或文档/工具小改，不改变用户操作语义或数据格式。
- `MINOR`：向后兼容的新功能，例如完成一个新应用或新增可选设置。
- `MAJOR`：需要用户迁移、硬件不兼容、破坏协议/文件格式或无法平滑升级的变化。
- 未完成实板验收的固件可以使用预发布标签，例如 `v0.2.0-rc.1`；稳定标签只能指向干净、可重建的提交。
- 使用 annotated tag，发布说明从 [`CHANGELOG.md`](CHANGELOG.md) 摘取，并附构建环境、FQBN、Flash/RAM、warnings、固件哈希、硬件标识和实板验收范围。

## 发布前清单

1. `git status --short` 为空，目标 commit 已审阅。
2. 运行 [`PROJECT_STATUS.zh-CN.md`](PROJECT_STATUS.zh-CN.md) 中全部不接设备测试，正式固件使用 `board.ps1` 真编译。
3. 更新中英文入口、`CHANGELOG.md` 和项目状态；没有把主机测试误写成实板结论。
4. 若改硬件，核对 `.eprj2` 哈希，并从该版本重新生成制造文件；若改协议/格式，更新对应规范与兼容测试。
5. 在经过确认的设备和端口做范围明确的实板验收；记录硬件 revision、commit、供电/扬声器条件。
6. 为要发布的二进制生成 SHA-256；确认不含个人文件、凭据或受限音乐内容。
7. 创建 tag 和 GitHub Release 后，确认仓库默认分支、源码归档和发布附件一致。

不要为了“看起来有版本”给旧提交追溯补标签。先把当前已验收功能和仓库整理推入 `main`，再按发布清单决定第一个公开版本号。
