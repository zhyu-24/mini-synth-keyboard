# 小电子琴 · 收板后硬件测试与首次上电指南

> 于子涵 / 建立于 2026-08-09
>
> **用途：板子完成贴片、手焊或返修后，按阶段验证硬件。**
>
> 以后在新会话中说“开始小电子琴硬件测试”“调用小电子琴测试文档”或直接给出本文件路径，Nia 应先完整读取本文，再根据当前装配状态只指导眼前一步。
>
> **本文是硬件测试阶段的现行入口。** 若本文与 `05-pcb-workflow.md` 早期测试段落冲突，以本文为准；器件和网络定义仍以 `00-design-decisions.md`、`02-bom-footprints.md` 为准。

---

## 一、测试点速查

| 测试点 | 网络 | 静态预期 | 主要用途 |
|---|---|---|---|
| TP1 | `+5V` | 接近实际 USB/VBUS 输入电压 | 检查 Type-C、输入路径和 5V 供电 |
| TP2 | `+3V3` | 稳定接近 3.3V | 检查 AMS1117、3.3V 短路/过载及动态跌压 |
| TP3 | `GND` | 公共参考地 | 万用表黑表笔、示波器探头地 |
| TP4 | `EN` | 平时接近 TP2；按 RESET 时接近 0V，松开后恢复 | 判断 ESP32 是否被卡在复位态；观察 EN 上升过程 |
| TP5 | `BOOT` | 平时接近 TP2；按 BOOT 时接近 0V，松开后恢复 | 判断 GPIO0 是否被持续拉低、芯片是否误进下载模式 |

**测直流电压时：**万用表置直流电压档，黑表笔固定在 TP3，只移动红表笔。优先用夹子固定黑表笔，避免两支探针同时滑动造成短路。

---

## 二、准备的工具

### 最低配置

- 数字万用表；
- 放大镜或显微镜；
- 稳定的 5V 电源或 USB 供电；
- 绝缘工作垫；
- 可固定在 TP3 的黑表笔夹。

### 推荐配置

- 带可调限流和电流显示的台式电源；
- 双通道示波器；
- 短接地弹簧或尽可能短的探头地线；
- 热电偶、红外测温仪或热像仪；
- USB 电流表；
- 串口工具或 USB 下载环境。

不要只用“手摸温度”作为热测试结论。手摸只能发现非常明显的异常，不能给出可重复的温升数据。

---

## 三、通电前检查

在每次首次上电、重大返修或更换电源器件后执行。

### 1. 目视检查

- Type-C、AMS1117、ESP32 模块、NS4168 有无偏位、连锡、虚焊；
- 焊盘间有无锡珠、剪下的引脚、导线碎屑；
- 连接器方向是否正确；
- OLED 和喇叭暂不连接；
- PCB 放在绝缘表面，背面没有金属物压住焊点。

### 2. 断电测量

- 测 `+5V` 对 `GND`、`+3V3` 对 `GND`；
- 重点判断是否存在**稳定的近零欧硬短路**；
- 电容充电会让读数随时间变化，因此不要把某个固定电阻值机械地当作合格线；
- 若蜂鸣器持续鸣叫或阻值稳定接近表笔短接值，先排查短路，不要通电。

### 3. 与装配相关的专项检查

- 插喇叭前，按原计划确认 J5 固定片与信号脚之间没有意外导通；
- 插 OLED 前逐脚核对 `VCC/GND/SDA/SCL`，不能只按插座外形猜方向；
- SW10/11 用蜂鸣档确认脚组：同组常通，不同组按下才通。

---

## 四、阶段 A：第一次限流上电——只检查电源树

### 装配状态

- OLED 不接；
- 喇叭不接；
- 功放保持硬件默认关闭；
- 不运行最大负载固件。

### 操作

1. 若使用台式电源，从较低限流开始观察；进入限流、输出电压被拉低或电流明显异常时立即断电。不要为了“让它启动”盲目把限流调大。
2. 上电后先看是否冒烟、异味、异常声响或器件迅速升温；有任一现象立即断电。
3. 黑表笔固定 TP3，依次记录 TP1、TP2。
4. 记录输入电流以及 U2、U1、U3 的温度或升温趋势。

### 判定

- TP1 应接近供电端实际 VBUS。明显为 0V或大幅低于电源端时，先查 Type-C、CC 电阻、焊接和输入路径。
- TP2 应稳定接近 3.3V。
- **若 TP2 高于 3.6V、低于 3.0V、持续跳变，或 U2 快速异常发热，应立即断电排查。**
- `3.0V` 是 ESP32-S3 模块和 NS4168 的工作下限，不代表“刚好高于 3.0V 就有充足设计余量”。
- 项目旧文档中的 `3.235～3.365V` 来自既有器件资料记录；正式把它当作严格验收范围前，应再次锁定 C6186 对应的实际厂商、完整 MPN、datasheet 版本和测试负载条件。

### 记录

| 项目 | 测量值 | 状态/备注 |
|---|---:|---|
| 电源端输入 |  |  |
| TP1 `+5V` |  |  |
| TP2 `+3V3` |  |  |
| 输入电流 |  |  |
| U2 温度/升温 |  |  |
| U1 温度/升温 |  |  |
| U3 温度/升温 |  |  |

---

## 五、阶段 B：检查 RESET 与 BOOT

前提：TP1、TP2 已通过阶段 A。

### TP4：EN / RESET

1. 不按 RESET，测 TP4：应接近 TP2；
2. 按住 RESET：应降到接近 0V；
3. 松开：应恢复到高电平；
4. 若有示波器，可观察松开或上电后的 EN 上升过程，应单调上升并最终稳定，不应反复跌落。

若 TP4 一直是 0V，优先检查：

- SW11 脚组是否接错；
- EN 焊桥；
- R6/C7 焊接；
- EN 网络是否被其他器件拉低。

### TP5：BOOT / GPIO0

1. 不按 BOOT，测 TP5：应接近 TP2；
2. 按住 BOOT：应降到接近 0V；
3. 松开：应恢复到高电平。

若 TP5 一直是 0V，芯片可能持续进入下载模式。优先检查 SW10 脚组、R7 和 GPIO0 网络，不要先把问题归咎于固件。

| 项目 | 未按按键 | 按住按键 | 松开后 | 结果 |
|---|---:|---:|---:|---|
| TP4 `EN` |  |  |  |  |
| TP5 `BOOT` |  |  |  |  |

---

## 六、阶段 C：验证 ESP32 启动与下载

前提：电源、EN、BOOT 均正常。

1. 先验证电脑是否识别 USB 设备或串口；
2. 记录下载模式能否通过 BOOT + RESET 正常进入；
3. 烧录最小测试固件：串口输出、LED 或简单按键扫描即可；
4. 反复冷启动和按 RESET，确认没有偶发启动失败；
5. 若出现反复重启，同时观察 TP2 和 TP4：
   - TP2 下陷，优先查电源和负载；
   - TP4 被拉低，优先查复位网络；
   - 两者都正常，再查启动绑带和固件。

不要一开始就运行 OLED、I2S、Wi-Fi 和全部按键的综合程序。先用最小固件证明 MCU 基础链路正常。

---

## 七、阶段 D：逐个接入外设

每次只增加一个变量，新增设备工作正常后再进行下一步。

### 1. OLED

1. 断电；
2. 再次逐脚核对 `VCC/GND/SDA/SCL`；
3. 接入 OLED；
4. 上电后复测 TP2；
5. 运行仅包含 I2C 扫描和简单显示的固件；
6. 若不显示，依次查供电、地址、SDA/SCL、电平和上拉，不先怀疑屏幕损坏。

### 2. 按键

- 逐个按下 SW1～SW9，记录是否有漏触发、常按下或键位错配；
- 同时验证 SW10 BOOT、SW11 RESET；
- 软件显示的键位必须与 `00-design-decisions.md` 的现行映射一致。

### 3. 功放与喇叭

1. 先确认 J5 与喇叭实物兼容、无短路；
2. 断电插入喇叭；
3. 固件启动后先保持 `AMP_CTRL=0`；
4. I2S 配置完成后再拉高 `AMP_CTRL`；
5. 从低音量、单音开始，确认无明显爆音、断续或异常发热；
6. 再逐步提高音量。

若无声，按“3.3V 电源 → AMP_CTRL → BCLK/LRCLK/SDATA → J5/喇叭”顺序排查，不要同时改多个固件参数。

---

## 八、阶段 E：示波器检查 3.3V 动态稳定性

### 先看绝对最低电压：DC 耦合

要判断 3.3V 是否在播放、无线发射或启动瞬间跌破 3.0V：

- 示波器使用 **DC 耦合**；
- 探头尖端接 TP2，地接 TP3；
- 使用短接地弹簧或尽可能短的地线；
- 触发捕获启动、最大音量和无线发射时的最低值；
- 记录 `Vmin`，而不是只看屏幕上的平均电压。

**硬性功能下限：Vmin 不得跌破 3.0V。**若最低值只是略高于 3.0V，也应视为余量不足，结合是否重启、温升和重复测试判断，而不是简单写“通过”。

### 再看小纹波：AC 耦合

AC 耦合用于滤去 3.3V 直流分量、放大观察纹波和尖峰：

- 可开启约 20MHz 带宽限制，减少探头拾取的高频杂讯；
- 记录纹波峰峰值、主要频率和测试工况；
- AC 耦合画面不能直接给出电源的绝对最低电压，不能拿它单独判断是否跌破 3.0V。

### 推荐工况

1. 空闲；
2. OLED 刷新；
3. 低音量单音；
4. 最大允许音量；
5. Wi-Fi/BLE 活动；
6. 最大音量 + 无线活动的组合峰值；
7. 冷启动。

| 工况 | TP2 平均值 | TP2 最低值（DC） | 纹波 Vpp（AC） | 是否复位/失真 | 备注 |
|---|---:|---:|---:|---|---|
| 空闲 |  |  |  |  |  |
| OLED 刷新 |  |  |  |  |  |
| 低音量 |  |  |  |  |  |
| 最大音量 |  |  |  |  |  |
| 无线活动 |  |  |  |  |  |
| 最大音量 + 无线 |  |  |  |  |  |
| 冷启动 |  |  |  |  |  |

---

## 九、功放输出测量的安全边界

NS4168 的 `VoP`、`VoN` 是桥接输出，**两端都不是 GND**。

- 禁止把普通台式示波器的地夹夹到 VoP 或 VoN；
- 普通示波器各通道的地通常相通并接保护地，错误连接可能把功放输出直接短路；
- 推荐使用差分探头；
- 或用双通道分别测 `VoP 对板载 GND`、`VoN 对板载 GND`，再用数学功能计算 `CH1-CH2`；两个探头地都只接板载 GND。

拿不准示波器是否隔离时，先停下确认仪器接地结构，不试探性夹上去。

---

## 十、阶段 F：温升与长时间稳定性

在基础功能和动态电源测试通过后再做。

1. 记录环境温度；
2. 分别运行空闲、持续播放、最大允许音量、无线活动等工况；
3. 每隔固定时间记录 U2、U1、U3 温度；
4. 观察是否出现重启、声音失真、OLED 花屏、按键失灵或 USB 断连；
5. 断电后检查焊点、连接器和喇叭固定是否松动。

测试时长和温度合格线应根据最终外壳、环境温度、实测负载及器件资料决定。第一轮先完整留数据，不凭“摸起来还能接受”宣布通过。

| 时间 | 环境温度 | U2 | U1 | U3 | 当前工况 | 异常现象 |
|---|---:|---:|---:|---:|---|---|
| 0 min |  |  |  |  |  |  |
| 5 min |  |  |  |  |  |  |
| 15 min |  |  |  |  |  |  |
| 30 min |  |  |  |  |  |  |

---

## 十一、常见现象的第一排查方向

| 现象 | 第一排查方向 |
|---|---|
| TP1 无电压 | USB 供电端、Type-C 焊接、CC 电阻、输入路径 |
| TP1 正常、TP2 为 0V | 3.3V 硬短路、U2 引脚/焊接、U2 损坏 |
| TP2 偏低且 U2 很热 | 3.3V 过载或短路、LDO 热限制、负载异常 |
| LED 亮但程序不运行 | 先量 TP4/TP5，区分 EN 卡低和 BOOT 卡低 |
| 反复重启 | TP2 瞬态跌压、EN 波形、启动绑带、固件看门狗 |
| OLED 不亮 | 插针方向、供电、I2C 地址、SDA/SCL、上拉 |
| 功放无声 | `AMP_CTRL`、I2S 三线/时隙、J5、喇叭 |
| 放音时复位 | 3.3V 瞬态凹陷、U2 温升、音量过高、组合峰值负载 |
| 示波器看到很多尖峰 | 先缩短探头地线并限制带宽，再判断是不是板上真实噪声 |

---

## 十二、每次测试的总记录

- 日期：
- PCB 版本：
- 当前焊接/返修状态：
- 供电方式：
- 限流设置：
- 固件版本或测试程序：
- 已连接外设：
- 使用仪器及探头：
- 本轮只改变的变量：
- 测试结果：
- 异常现象：
- 下一步：

**纪律：一次只改变一个变量。**如果测试中改了焊接、固件、供电或外设连接，应另起一轮记录，避免出现“恢复正常了但不知道是哪项改动起作用”的情况。
