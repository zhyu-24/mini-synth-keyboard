# 小电子琴 · PCB 阶段完整工作流

> 建立 2026-08-07 23:45 · 于子涵 + Nia
> **本文用途**：PCB 阶段的唯一流程依据。新会话开始 PCB 工作时先完整读本文，再按需查 `00-design-decisions.md`（设计决策）与 `02-bom-footprints.md`（BOM/封装）。
>
> **所有菜单路径均已从嘉立创EDA专业版官方文档核实**（`prodocs.lceda.cn`），非凭记忆撰写。若实际菜单与本文不符，说明软件版本已更新——以软件实际为准，并回来更新本文。
>
> ⚠️ **本文针对专业版**（`pro.lceda.cn/editor`）。标准版菜单完全不同，不适用。

---

## 零、快捷键与通用操作速查

| 操作 | 快捷键 | 说明 |
|---|---|---|
| 保存全部 | `Ctrl+S` | 养成习惯，每完成一步就存 |
| 单路布线 | `Alt+W` | 专业版主题配置 |
| 铺铜区域 | `E` | |
| 重建铺铜 | `Shift+B` | 改完铺铜属性后必按 |
| 测量距离 | `Alt+M` | **本项目会频繁用到**（量通道宽度、器件间距） |
| 单层显示 | `Shift+S` | 高亮当前层、隐藏其他层。**仅视觉隐藏，不影响 Gerber 输出** |
| 对齐网格 | `Ctrl+Shift+G` | |

**切换活跃层**：右侧面板「图层」栏 → 点该层的**颜色标识区**，使铅笔图标切到该层。也可在图页底部栏切换。
- 点「眼睛」图标 = 显示/隐藏该层
- 点「锁」图标 = 锁定该层，该层元素无法被鼠标移动（**摆完键区后锁定，防手滑**）

---

## 一、流程总览（官方顺序 + 本项目特化）

嘉立创官方 wiki 教程给出的顺序原文是：
> 「网表导入 → 结构导入（确保板框大小、结构器件信息、禁布区、限高区等信息的导入）→ 模块抓取 → 器件布局 → 规则设置 → 层叠设置 → PCB扇孔 → PCB走线 → DRC检查 → 丝印处理 → 光绘输出」

**⚠️ 但本项目要对"板框"这一步做特殊处理。** 官方流程假设板框尺寸是已知的外部约束（工业项目通常由机械工程师给定）。本项目板框高度**尚未确定**，且已明确「不预先定死板高，由摆件决定」（`00-design-decisions.md` 第五节）。

因此本项目采用的顺序是：

| 阶段 | 步骤 | 状态 |
|---|---|---|
| **准备** | 0. 设计规则 | ✅ **已完成**（08-07 23:41） |
| | 1. 原理图 DRC → 转 PCB | ✅ **已完成**（08-08 00:03） |
| | 2. 导入后验证（器件数、飞线） | ✅ **已完成** — 飞线 4 根确认、器件数核对（补记 TP4/TP5） |
| **定几何** | 3. 画**临时板框** + 设画布原点在其左下角 | ✅ **已完成**（08-08 01:15） |
| | 4a. 摆 **9 个轴座** → 翻底层 → 组合 → 锁定 | ✅ **已完成**（08-08 01:00）坐标/层向/焊盘网络均已核 |
| | 4b. 摆 **U1**（天线悬出左板框） | 🔄 **进行中** — 方案①已定（01:11），待实摆 |
| | 4c. 摆 **OLED 母座 + 4 孔** | ⬜ |
| | 5. 摆 **U3+C8/C9/C10（藏 OLED 下）/ J5（屏投影外）/ 喇叭占位框（U1↔OLED 间背面）** | ⬜ 方案已定，见 C2d |
| | 6. 摆 Type-C / U2+C1~C4 / 阻容 / SW10-11 / **TP1~TP5** | ⬜ **建议先划元件带分区**（C2d §5） |
| | 7. **量实际占用 → 定最终板框（只动上边右边）→ 倒圆角** | ⬜ |
| **布线** | 8. 禁铺区（天线净空） | ⬜ |
| | 9. 布线（电源优先 → 信号） | ⬜ |
| | 10. 底层铺铜 GND + **顶层元件带 `+3V3`（U2 散热，已从"可选"升为必需）** | ⬜ |
| **收尾** | 11. **E. 加键区 27 个机械孔 + OLED 4 孔** → DRC → 3D → 丝印 | ⬜ ⚠️ **可能改用 `KEYBOARD-SW` 封装免手加**，见 C1b |
| | 12. 导出 Gerber / 下单（**下单前走 DFM 查 0.49mm 孔距**） | ⬜ |

**为什么第 3 步画"临时"板框**：铺铜、禁铺区、部分 DRC 规则都需要板框存在才能工作，所以不能没有板框。但本项目板高由摆件结果决定，所以先画个明显够大的（例如 90×100mm），摆完再收到实际尺寸。**板框可以随时改，不必一次画对。**

---

## 二、阶段 A：导入原理图

### A1. 先在原理图里跑 DRC

**原理图编辑器 → 顶部菜单 `设计` → `检查DRC`**
- 结果在**底部面板的 DRC 栏**
- 若提示「建议使用器件标准化」→ 点左侧面板「器件标准化」处理

### A2. 转 PCB

**原理图编辑器 → 顶部菜单 `设计` → `更新/转换原理图到PCB`**

弹出「确认导入信息」窗口 → 默认勾选全部变更 → 点 **`应用修改`**

### A3. ⚠️ 以后改原理图用的是另一个入口

| 场景 | 入口 |
|---|---|
| 第一次转 | 原理图：`设计` → `更新/转换原理图到PCB` |
| **后续同步改动** | **PCB：`设计` → `从原理图导入变更`** |

**两个入口不同，别搞混。** 另注意：官方文档**未承诺**"绝不丢失已摆好的位置"，且勾选「同时更新导线网络」后部分走线网络可能需手动修正。**所以：尽量把原理图改动一次性做完再摆件，减少来回同步的次数。**

### A4. 导入后必须验证两件事

**① 器件个数对不对**
和 `02-bom-footprints.md` 逐项核对。**特别注意 TP1~TP3 测试焊盘**——文档里标为"自绘"，若封装还没画会缺失。

**② 飞线是不是都在（重要）**
飞线 = 细直线，表示"这两个焊盘应该连起来"。

> **这是验证跨页网络标签的最佳时机**：看 **U3 和 U1 之间是否有 4 根飞线**（对应 `AMP_CTRL` / `I2S_LRCLK` / `I2S_BCLK` / `I2S_SDATA`）。有 4 根 = 第三页四个标签全部配对成功。少一根 = 那个标签拼错了。
>
> 这比看 DRC 警告列表直观 —— 标签拼错在 DRC 里只报"单网络 warning"，不报 error，容易被 0 error 掩盖。

---

## 三、阶段 B：画临时板框 + 设原点

> 2026-08-08 00:08 重写，补齐"画在哪层"与专业版/标准版差异。菜单路径已从
> `prodocs.lceda.cn/cn/pcb/place-board-outline/`、`/cn/pcb/tools-layer-manager/`、
> `/cn/quick-start.html`、`/cn/pcb/place-canvas-origin/` 核实。

### B0. 板框在哪一层：**边框层（板框层）**

图层管理器官方定义原文：

> **边框层(板框层)**：板子形状定义层。定义板子的实际大小，板厂会根据这个外形进行生产板子。在Gerber时生成的GKO文件内。

所以答案是**边框层**，Gerber 里对应 **GKO** 文件——工厂就是照这个文件切板外形的。

### B1. ⚠️ 专业版**不需要**手动切层，这是与标准版的根本区别

网上大量教程（含嘉立创**标准版**官方文档）写的是：

> 「边框需在"边框层"绘制。**先切换至"边框层"**，再使用导线或圆弧进行绘制。」

**这是标准版的做法，专业版不适用，照着做会白忙。**

区别在于两版的对象模型不同：

| | 标准版 | **专业版（本项目）** |
|---|---|---|
| 板框是什么 | **画在边框层上的导线/圆弧** —— 层决定了它是板框 | **一种独立的对象类型**「板框」—— 类型决定了它是板框 |
| 怎么画 | 先切活跃层到边框层，再用导线画 | 直接 `放置` → `板框`，**不用管当前活跃层** |
| 同族对象 | — | 板框 / 铺铜区域 / 填充区域 / 挖槽区域 / 禁止区域，**五者绘制方式完全相同，靠"类型"区分** |

理解这一点顺带解释了后面几个阶段：阶段 D 的禁铺区、阶段 E 的挖槽孔、阶段 G 的铺铜，画法和板框一模一样，区别只是选了不同的对象类型。所以官方文档说 DXF 导入进来的线条**要手动"切换类型"**为板框或挖槽——类型是可以改的属性，不是层。

**结论：专业版就一句话——`放置` → `板框` → 选「矩形」，不用切层。**

### B2. 动手前先看一眼：是不是已经有一个板框了

转 PCB 时软件可能已自动生成过一个板框。**必须先确认，因为官方规则是：**

> 「一个板框内只能放置一个板框，**多余的板框会转为挖槽区域**。」

后果很具体：你以为画了第二个板框，实际得到的是一个**把板子挖穿的槽**。所以若已存在自动板框，**先删掉它再画**，或者干脆直接改它的尺寸（选中 → 右侧属性面板）。

### B3. 画：`放置` → `板框` → 矩形

官方提供**矩形 / 圆形 / 多边形**三种。**本项目选矩形**，两个理由：
1. 矩形工具画出来**天然闭合**。官方硬要求「板框必须闭合」，差一点缝隙工厂就报错，且**未闭合的板框无法倒圆角**（阶段 C6 要倒 R2~3mm）。多边形逐点点容易在收尾处留缝，还得多选线段再右键「合并为闭合对象」去补救。
2. 只有**矩形**板框能在右侧属性面板直接填圆角半径。

画完鼠标左键定形，选中后在**右侧属性面板**改精确尺寸——不用一次拖准。

**画之前确认画布单位是 mm**。若停在 mil 上，填「19.05」会得到 19.05 mil = **0.48mm**，键距直接错 40 倍。这个坑在下一步摆轴座时是致命的，现在就把单位确认好。

### B4. 临时尺寸：**90 × 100mm**

明显够大，摆完再收。**板框可以随时改，不必一次画对**——参见第一节"为什么画临时板框"。

### B5. 紧接着设画布原点（**这一步别跳过**）

**顶部菜单 `放置` → `画布原点`**，两种方式：
- **从光标**：鼠标变十字，点某个焊盘或符号中心即设为原点
- **从坐标**：弹窗里输 XY 数值

**把原点设在临时板框的左下角。**

为什么现在就做：下一步摆 9 个轴座要用 `布局` → `偏移` → `绝对偏移` 输坐标，"绝对"是相对画布原点的。原点在板框左下角，坐标就等于"距左边缘 / 距下边缘多少 mm"，心算可核对；原点在别处，你算出来的每个数都要额外做一次减法，错一个就是一颗键歪掉。

⚠️ **由此产生一条纪律：后面收最终板框时，只动上边和右边，保住左下角不动。** 否则原点相对板子的位置变了，已摆好的 9 个轴座坐标全部失去意义、要重算。本项目临时 90×100 → 最终约 (70~80)×87，正好是往上边和右边收，天然满足。

---

## 四、阶段 C：摆件（本项目核心难点）

### C0. 先设网格

**画布空白处点一下 → 右侧属性面板：`网格尺寸` / `栅格尺寸`**
或 **顶部菜单 `视图` → `网格`**

自定义常用值：**`设置` → `PCB/封装` → `常用网格尺寸` → 点 `+` 新增**，可填 **19.05**。

> 区别：**网格**是显示用的参考线，**栅格**是鼠标吸附的步距。摆键位时把栅格设成 19.05 会很省事。

### C1. 精确摆位的方法

⚠️ **9 个轴座必须按坐标精确摆放，绝对不能手拖。** 19.05mm 是刚性键距，手拖必然不准，后果是键帽互相干涉或装不进去。

**选中器件 → 顶部菜单 `布局` → `偏移` → `绝对偏移` → 输入坐标**

辅助工具：**`布局` → `对齐`**（多个器件对齐）

> 官方文档未列出右侧面板的器件坐标字段名，所以不要依赖"在右侧改 X/Y"这个说法——用 `布局 → 偏移 → 绝对偏移` 这条已核实的路径。

### C1b. 9 个轴座的坐标表（2026-08-08 00:15 推导）

> **状态：几何推断，未实测。** 依据 `00-design-decisions.md` 第五节的三项约束推出，用作**起始值**；摆完实测后可整块平移，不必重算。

**基准**：画布原点在临时板框左下角（B5）。板宽按 84.2mm、底边通道 8mm、键区 76.2×57.15mm 居中。

**纵向分层**（y 从下往上）：

| y 区间 | 内容 |
|---|---|
| 0 ~ 8 | 底边通道（A 束 4 根线） |
| 8 ~ 65.15 | 键区三排（57.15 = 19.05×3） |
| 65.15 ~ 87.15 | 元件带（≥22mm） |

⇒ 行中心：第三排 **y=17.525**、第二排 **y=36.575**、第一行 **y=55.625**
⇒ 列中心：**x=13.525 / 32.575 / 51.625 / 70.675**（左右各留 4mm 边距）

| 位号 | 键 | X | Y |
|---|---|---:|---:|
| SW1 | do | 13.525 | 36.575 |
| SW2 | re | 32.575 | 36.575 |
| SW3 | mi | 51.625 | 36.575 |
| SW8 | 播放/停止 | 70.675 | 36.575 |
| SW4 | fa | 13.525 | 17.525 |
| SW5 | sol | 32.575 | 17.525 |
| SW6 | la | 51.625 | 17.525 |
| SW7 | ti | 70.675 | 17.525 |
| SW9 | **Fn** | 70.675 | **55.625** |

**这套坐标在收板框后依然有效** —— 因为原点在左下角、收板框只动上边和右边（B5 纪律），左边距 4mm 和底边通道 8mm 都是相对左边和下边定义的，不受影响。

#### ⚠️ 先摆一颗，确认封装原点在哪

`绝对偏移` 把器件的**封装参考点**移到指定坐标，而**封装原点不一定是键位中心（φ4.0 中心孔）**。Kailh 热插拔座的封装含两个偏置的大焊盘 + 中心孔 + 两个定位孔，官方库的原点可能取"所有焊盘的中心"而非"轴体中心孔"。

**做法：先只摆 SW1 到 (13.525, 36.575)，用 `Alt+M` 量 φ4.0 中心孔的实际位置**，与目标值比对：
- 一致 ⇒ 原点就是键中心，其余 8 颗照表填
- 有固定偏移 (dx, dy) ⇒ **表里每个坐标都减去这个偏移**再填

好消息是这个错误**不致命**：9 颗偏移量完全相同，19.05 的相对键距不会被破坏，只是整块键区相对板子挪了位——而板框本来就要按摆件结果调。但必须知道偏了多少，否则判断"OLED 有没有压到第二排"时会用错基准。

#### ⚠️ 别指望 19.05 栅格能吸附到这些点

C0 建议把栅格设成 19.05，那是为了**目视和拖动**方便。但本表坐标含半个键距的偏移（9.525），**19.05 的栅格点上没有一个落在这些坐标上**。所以：**坐标一律用 `布局`→`偏移`→`绝对偏移` 输入，输完立刻锁定，之后不要再拖**。若发现某颗键"吸"到了整数栅格上，说明它被手拖过、已经错位。

#### ✅ 已实测（2026-08-08 00:26）：封装原点 = 底座几何中心

于子涵在 EDA 中实测确认：**Kailh 轴座封装的原点是底座几何中心，不是某个定位孔**。因为 MX 的 φ4.0 中心孔与两个 φ1.7 定位孔左右对称，几何中心自然落在中心孔上 ⇒ **原点 = 中心孔 = 键帽中心**，上表坐标可直接填入，无需偏移修正。

同时观察到：**两个大焊盘相对中心呈对角分布**（一个偏左上、一个偏右下），pin1 = `KEY_xx`、pin2 = `GND`。**焊盘不对称是下一条的全部风险来源。**

#### 🔴 轴座必须在**底层**，且必须验证镜像方向

**观测（00:27 于子涵）：转 PCB 后 9 颗轴座全部在顶层。** 这是原理图转 PCB 的默认行为（所有器件先落顶层），需要处理。

**为什么必须在底层**：热插拔的工作方式是——轴体从**顶面**插入，两根针脚**穿过 PCB**，被焊在**底面**的轴座夹住。`00-design-decisions.md` 已记录该件为"背面贴装件、板面以上凸出约 1.8mm"，正是喇叭不能贴键区背面的原因。**轴座在顶层则轴体根本无法插入。**

**操作**：框选 9 颗 → **`布局` → `翻面`**。官方说明：「会把可以翻转的图元自动翻转到对面层去，**并会自动镜像**」。

⚠️ **"自动镜像"这四个字是最大的风险点，必须验证：**

封装在 EDA 里永远以**顶视图**显示。轴体的两根针脚在顶视图下的位置是**固定且不对称**的（MX 标准，两针呈对角）。因此**焊盘在顶视图下的 X-Y 位置必须与针脚位置重合**——翻面会把这个位置左右镜像，**镜像后就对不上了**。

两种可能，必须二选一确认：

| 若立创官方封装 C41430893 是…… | 则正确做法 |
|---|---|
| 按**底面贴装**绘制（已预先考虑镜像） | 当前在顶层是错的，**翻面同时修正层和镜像** ✅ |
| 按**顶面贴装**绘制 | 翻面会把本来正确的方向镜像坏，需要翻面**再**镜回来 |

**这是本阶段唯一"错了整个键区报废、且 DRC 完全不报"的项。** 错误只会在拿到实物插轴时暴露——9 颗全部插不进，板子直接废掉。

**验证方法（不要凭记忆判断 MX 脚位）**，按可靠性排序：
1. **查轴体（不是轴座）的数据手册**中的 PCB 焊盘图 / 顶视图针脚坐标，注意图纸标的是顶视还是底视，与 EDA 顶视图直接比对
2. **oshwhub 上的开源热插拔键盘工程**（README 第一节已认可这种参考用法）：看它的轴座在哪层、焊盘对角方向与我们是否一致
3. 轴体到手后拿实物比对 EDA 打印图

**翻面不影响坐标**：原点是几何中心、镜像也是对该中心做的，9 颗的绝对坐标保持不变，不必重新输入。

#### ✅ 镜像方向已交叉验证通过（2026-08-08 00:31）

**判据来源**：KiCad 官方封装库 `Button_Switch_Keyboard.pretty/SW_Cherry_MX_1.00u_PCB.kicad_mod`（描述行注明依据 Cherry MX 产品目录 `cherryamericas.com/.../mx_cat.pdf`）。该文件在 `F.Cu`（顶层），即**顶视图**坐标，可与 EDA 顶视图直接比对。

原始数据：

```
pad 1        at (0,     0   )   ← 焊盘
pad 2        at (-6.35, 2.54)   ← 焊盘
np_thru_hole at (-2.54, 5.08) size 4.0    ← 中心孔
np_thru_hole at (-7.62, 5.08) size 1.7    ← 定位孔
np_thru_hole at ( 2.54, 5.08) size 1.7    ← 定位孔
```

**换算成以中心孔为原点的顶视图**（KiCad 的 +Y 向下，故换算时 Y 取反）：

| 元素 | 相对中心孔（顶视图，+Y 向上） |
|---|---|
| 中心孔 φ4.0 | (0, 0) |
| 定位孔 φ1.7 | **(−5.08, 0)** 与 **(+5.08, 0)** |
| 针脚 A | 左 3.81、**上 2.54** |
| 针脚 B | 右 2.54、**上 5.08** |

⇒ **两根针脚都在中心孔的上方（北侧），且左边那根低、右边那根高 = 「左低右高」。**

**与子涵 00:31 的独立判断一致**（"正面的轴体安装的时候应该就是左低右高的"），且与翻面**后**的观察一致 ⇒ **立创封装 C41430893 是按底面贴装绘制的，翻面到底层同时修正了层和镜像，一步到位。**

> 三条独立信源交叉：① KiCad 官方库（引用 Cherry 目录）② 子涵对实物安装方向的判断 ③ 翻面后 EDA 中的实际显示。实物到手后仍应比对一次，但不阻塞继续摆件。

#### 为什么"镜像错了"无法靠旋转补救（所以必须一次做对）

MX 的中心孔与两个定位孔**三孔共线且左右对称**，所以轴体**可以 0° 或 180° 两种朝向插入，机械上都装得进**（键盘圈的 "north-facing / south-facing" 就是这回事）。这容易让人以为方向错了转一下就行。

**但镜像和旋转是两回事：**

| 操作 | 针脚落在哪 |
|---|---|
| 正确（0°） | 都在**北**侧，左低右高 |
| 轴体转 180° | 都在**南**侧，右低左高 |
| **封装被 X 镜像** | 都在北侧，但**左高右低** |

镜像的结果是**手性翻转**，任何旋转都得不到 —— 转 180° 只会把针脚甩到南侧，还是对不上。**所以镜像错了就是错了，只能改封装，板子已下单则报废。**

#### 副产品：这决定了按键怎么布线（阶段 F 的伏笔）

轴座在底层 ⇒ 18 个焊盘都在底层，而底层是整片 GND 铺铜（阶段 G）。由此得到一个很干净的分工：

- **pin2 (`GND`) 零成本**：焊盘就在 GND 平面所在层上，**直接被铺铜连上，一个过孔都不需要**
- **pin1 (`KEY_xx`) 走顶层**：在焊盘处打一个过孔上到顶层，在顶层走到 U1（U1 也在顶层）

**为什么不干脆在底层走按键线**：那 9 根线会把整片 GND 平面切开，而键区底层还被 φ4.0 + φ1.7 孔切碎，回流路径会被逼着绕远。**键区顶层是全板最空的区域**（顶层这里什么都没有），白送的布线空间不用是浪费。代价仅 9 个过孔 + 9 个 antipad，对 GND 平面的破坏可以忽略。

⇒ 修正上一轮我说的"键区底层会被切得比较碎"：**只要按键信号走顶层，就不会。**

#### 🔴 已确认（00:39）：封装只含 2 个针脚孔，**φ4.0 中心孔 + 2× φ1.7 定位孔必须自己加**

**观测（00:39 于子涵）**：PCB 图与 3D 图中都只有**两个金属脚的孔**，无中心导柱孔。

**解读**：那两个孔是**轴体针脚穿板孔**（KiCad 官方库为 drill φ1.5 / pad φ2.2），属于封装的 pad，**带 drill 属性的 pad 会自动进钻孔文件，板厂会打——这两个不用管**。

**但缺三个孔**，且它们**不属于轴座、属于轴体**，所以任何轴座封装都不会带：

| 孔 | 服务于 | 相对键位中心 |
|---|---|---|
| φ4.0 | 轴体**中心导柱**（3 脚 / 5 脚**都有**） | (0, 0) |
| φ1.7 | 轴体**定位柱**（**仅 5 脚有**） | (−5.08, 0) |
| φ1.7 | 同上 | (+5.08, 0) |

⚠️ **缺孔不会被任何检查发现**：DRC 不报（缺孔非电气错误）、3D 预览正常（只渲染轴座、不渲染未装的轴体）。**只有拿到实物插轴时才暴露，此时板子已报废。**

#### ✅ 决策：维持 5 脚轴体，不改 3 脚（2026-08-08 00:39）

子涵已按建议采购 5 脚（PCB mount）轴体，问是否趁未发货改 3 脚以省掉打孔。**结论：不改。** 三条理由：

1. **改 3 脚省不掉主要工作。** **中心导柱是 MX 的标准结构，3 脚和 5 脚都有** —— 3 脚（plate mount）少的只是两个塑料定位柱，中心导柱照旧。所以 φ4.0 中心孔无论如何都要开，改 3 脚只省掉 2×9 = 18 个 φ1.7 孔，而 φ4.0 那 9 个（尺寸最大、最占通道）一个都省不掉。
2. **打孔的边际成本几乎为零。** 孔在 EDA 里画一组、复制、按 19.05 的倍数平移即可；PCB 厂对这个孔数量级不额外收费；φ1.7 / φ4.0 都在常规工艺内。**为省画图时间换掉更好的机械方案，不划算。**
3. **5 脚在热插拔场景下明显更优，这是真正的理由。** 热插拔的受力方式与焊死完全不同：轴体靠两根针脚插在底面夹子里（插入力 3KG max、5000 次插拔寿命），拔轴时是**人手在板面上方施加的拉力和扭力**。5 脚的两个定位柱在中心两侧 ±5.08mm 提供**抗旋转力臂**；3 脚只有一根中心柱，绕中心轴的旋转几乎无约束 ⇒ 拔轴时轴体易歪、针脚易弯，而弯针脚同时损伤轴体和夹子。**本项目明确要反复换轴体试手感**（`02-bom-footprints.md`：红轴/茶轴各买几颗试），插拔次数远高于普通键盘，抗旋转正是最需要的。

> 附带：改单还有交付延迟风险，而当前不阻塞任何工作。

#### 针脚孔实测（00:49 澄清）：**φ3.0 是孔径，孔本身不是焊盘**

**观测（于子涵）**：那两处**没有焊盘的蓝色填充、不是焊盘**；3mm 是**板上实开的孔径**；孔内侧另有小的圆角矩形（= 轴座的 SMD 焊盘）用于夹住轴体针脚。

**这就是标准的 MX 热插拔封装结构**，与焊死式（KiCad `SW_Cherry_MX_1.00u_PCB`：drill φ1.5 / pad φ2.2）完全不同：

| | 焊死式 | **热插拔式（本项目）** |
|---|---|---|
| 针脚穿板孔 | φ1.5 **PTH 焊盘**，针脚直接焊在孔里 | **φ3.0 大孔，非焊盘** —— 只是让针脚自由穿过 |
| 电气接触点 | 就是这个孔 | **轴座底面的 SMD 焊盘**（圆角矩形），针脚穿过后被弹片夹住 |
| 为什么孔要 φ3.0 | — | 弹片要在孔下方占位，针脚还要有插拔余量 |

#### ❌ 撤回上一轮结论：过孔省不掉

00:45 我据"PTH 带网络"推出"顶层孔环即走线点、省 9 个过孔"。**该结论作废** —— 孔不是焊盘就没有孔环，也不带网络。

**电气连接点是轴座底面的 SMD 焊盘 ⇒ 它只存在于底层 ⇒ 按键信号要上顶层，必须打过孔。** 回到 C1b 原方案：pin1 处打过孔上顶层、在顶层走到 U1；pin2(`GND`) 焊盘直接由底层铺铜连上。

> 记一笔：`是金属的`这类描述不足以判定"是焊盘/带网络"。判据只有一条 —— **右侧属性面板里有没有网络字段**。下次直接问这个。

#### 🔴 由 φ3.0 推出的真问题：与 φ1.7 定位孔只剩 **0.49mm** 板材

孔径确认为 φ3.0 后，几何上出现一处贴着工艺下限的间隙。用 KiCad 官方库的 MX 标准坐标算（顶视图、以中心孔为原点）：

| 孔对 | 中心距 | 边到边 | 判断 |
|---|---:|---:|---|
| 针脚孔 A (−3.81, 2.54) ↔ **左定位孔 (−5.08, 0)** | 2.84 | **0.49mm** | ⚠️ **临界** |
| 针脚孔 B (2.54, 5.08) ↔ 右定位孔 (5.08, 0) | 5.68 | 3.33mm | ✅ |
| 针脚孔 A ↔ 中心孔 φ4.0 | 4.58 | 1.08mm | ✅ |
| 针脚孔 B ↔ 中心孔 φ4.0 | 5.68 | 2.18mm | ✅ |

> 算法：中心距 − 3.0/2 − 1.7/2。A 的中心距 = √(1.27² + 2.54²) = 2.840。

**嘉立创工艺参数页标「插件孔孔边到孔边最小间隙 0.45mm」**（`jlc.com/portal/vtechnology.html`，**单一来源，下单前应复核**）。

⇒ **0.49 > 0.45，通过，但余量仅 0.04mm。** 而常规插件孔公差是 **+0.13 / −0.08mm**，最坏情况两孔同时偏大即跌破 0.45。

**但这不是本项目的设计缺陷，而是 MX 热插拔封装的固有几何** —— 上述坐标全部由轴体机械结构决定，无一可调。业内大量热插拔键盘 PCB 正是这个尺寸在嘉立创量产。

**⚠️ 验证时机：必须等阶段 E 加完 27 个孔之后**（00:57 于子涵指正）。

φ1.7 定位孔此刻尚不存在，现在跑 DRC 只会检查两个 φ3.0 针脚孔（相隔大半个键位），**必然 0 error，且这个 0 error 毫无意义**。原文写"现在跑 DRC 验证"是把"该验证"与"现在能验证"混为一谈，属逻辑错误。

**处理方式（阶段 E 加完孔后执行，顺序不可颠倒）**：

1. **先用 `Alt+M` 手量**：左侧 φ3.0 针脚孔边缘 → 左侧 φ1.7 定位孔边缘的实际距离，与本表手算的 0.49mm 比对。**必做，不可由 DRC 替代**（理由见下）。
2. **再跑 DRC**，设计规则中「孔到孔间距」设 0.45mm。
3. 若判定不合规：把 φ1.7 改为 **φ1.6**（边到边 0.49 → 0.54mm）。定位柱是塑料件、公称约 1.6mm，φ1.6 偏紧但通常可插；**实物轴体到手后试装确认**。
4. **不要**为此把 φ3.0 改小 —— 那是弹片和插拔余量决定的，改小会导致轴体插不到底。

> **为什么手量不能省** —— 三孔是用**挖槽区域**画的（非焊盘）。DRC 的「孔到孔间距」规则**是否覆盖挖槽区域尚未确认**：许多 EDA 该规则只检查焊盘/过孔的钻孔，挖槽区域按板形加工走另一套规则、甚至不参与检查。若如此，**DRC 显示 0 error 不代表间隙合规，只代表它没看**。
>
> 因此第 2 步的真正价值是**摸清 DRC 的检查边界**：手量 0.49mm 而 DRC 沉默 ⇒ 该规则不覆盖挖槽区域 ⇒ **今后所有涉及挖槽区域的间距一律手量**。这个结论对阶段 E 的 OLED 4 个安装孔（同为挖槽区域）同样适用。测出结果请回填本节。

#### 对「5 脚 vs 3 脚」决策的影响：**不翻转，但记录反方证据**

诚实记录：**改 3 脚确实能消除这个 0.49mm 临界间隙**（3 脚无定位柱 ⇒ 无 φ1.7 孔 ⇒ 最紧处变成针脚孔↔中心孔的 1.08mm，非常宽松）。这是 00:39 决策时未知的反方证据。

**但结论仍维持 5 脚**：
- 0.49mm **通过**嘉立创常规工艺，且是业内标准做法，不是踩红线
- 若 DRC 报警，还有 φ1.6 这一步可退
- 而抗旋转的收益在"反复换轴试手感"这个明确需求下是持续性的，临界间隙则是一次性可验证项

⇒ **触发条件**：若上述第 1、2 步都无法把间隙做合规，再重启 5 脚/3 脚讨论。

#### 待确认：pin1 的网络必须是 `KEY_xx`，不能是 `GND`

按 `00-design-decisions.md`，轴座两脚是 **pin1 = `KEY_xx`、pin2 = `GND`**；00:31 截图显示 pin1 标 `KEY_DO`、pin2 标 `GND`，看起来正常。**但请确认这两个 SMD 焊盘（不是孔）各自的网络**。

若两个焊盘都在 `GND` 上，等于按键被永久短路、按下毫无变化，且 **DRC 不报错**（GND↔GND 是合法连接）。这类错误只能靠读网络名发现。

#### 由此产生的底层铺铜影响（阶段 G 记得）

键区底层要挖掉：9× φ3.0 针脚孔 ×2 = 18 个 φ3.0、9× φ4.0 中心孔、18× φ1.7 定位孔、9 个 `KEY_xx` 过孔的隔离环，外加 18 个轴座 SMD 焊盘。**键区底层 GND 会被切得相当碎。**

**但不构成问题**，因为按键信号全部走顶层（C1b 分层策略），底层在键区只承担回流，回流可绕开这些洞 —— 前提是**别再往底层加走线**。这条纪律到布线阶段必须守住。

#### 🔴 独立核查结论（01:28，四路交叉验证之一）—— 三条修正 + 一条重大发现

> 核查方式：独立 subagent 查 CHERRY《MX Series》官方规格书图 10-3、Kailh 官方规格书、嘉立创 EDA 器件库、Acheron 开源封装库。

**① 物料身份更正**：**C41430893 登记厂牌是 HanElectricity（瀚源），不是 Kailh。** 型号兼容 Kailh 类轴座，但**不能把 Kailh/PG 规格书当作该批料的原厂保证**。`02-bom-footprints.md` 与本文此前均写作"Kailh CPG151101S11"，需修正为"CPG151101S11 兼容型，瀚源"。

**② ⭐ 重大发现：嘉立创有含全部孔的完整键位封装，不必手加 27 个孔。**

| | 编号 / UUID | 内容 |
|---|---|---|
| 当前用的（只有轴座） | C41430893 | 仅 2× Ø3.0 针脚孔 |
| **推荐改用** | 封装名 **`KEYBOARD-SW`**，库 UUID **`5d1d41ce093e4ac5add02ca97714969c`** | 含中心 Ø4.0 + 两定位孔（约 **Ø1.905**）+ 2× Ø3.0 轴座孔/焊盘 |

⇒ **本节"必须自己加 27 个孔"的前提被推翻。** 换封装比手加 27 个孔可靠得多（手加漏一个 = 那颗键装不上，且 DRC/3D 均不报）。

⚠️ **但换封装前必须在副本工程里实测复核**，两个疑点：
- 定位孔标为 **Ø1.905**，而 CHERRY 官方是 **Ø1.702**（Ø.067in）—— 差 0.2mm。偏大更好插，但要确认不会与针脚孔间距冲突
- 该封装是否已按底面贴装绘制、层向如何

**③ 定位孔官方尺寸更正**：CHERRY 官方图 10-3 标定位孔 **Ø.067in = 1.702mm**（我此前写 1.7，四舍五入无误）；中心孔 **Ø.157in = 3.988mm**；针脚孔 **Ø.059in = 1.499mm**。KiCad 库的 4.0/1.7/1.5 与官方相符 ✅。

**④ 「φ1.6 退路」不成立** ❌：官方只给推荐定位**孔** Ø1.702，**未标定位柱外径**。Ø1.6 低于标称，不能保证所有 5 脚轴可插入。**不得作为未经实测的退路。** ⇒ 若 §"0.49mm 间隙"需要退路，只能靠实测轴体定位柱实际外径后再定。

**⑤ 0.49mm 的算术已验算通过** ✅：0.4898 / 3.3296 / 1.0780 / 2.1796 mm，四项全对。

**⑥ 「业内量产常态」这句我没有依据** ⚠️：未找到量产统计。但 **Acheron 开源封装库**（成熟项目，`github.com/AcheronProject/acheron_MXH.pretty`）用同坐标 + Ø3.048/Ø1.702，最近边距约 **0.465mm** —— 说明该几何**不是孤例**，但"大量在嘉立创量产"属我臆测，撤回。

**⑦ 嘉立创 0.45mm 的适用范围要收紧** ⚠️：该数字确实存在于官方工艺页，但出现在**「有铜插件孔焊环」**段落；官方**未单列 NPTH↔NPTH 孔距**。我们这两个孔是**无铜机械孔**，⇒ **不能据 0.45 作"必通过"承诺**。下单前应直接问嘉立创客服或走一次在线 DFM 检查。

**⑧ 「5 脚必然更优」过度断言** ❌：两个定位柱确有抗扭作用，**在无定位板（plate）时有价值**；但有合格定位板的热插拔键盘用 3 脚轴是正常做法。**不能说"热插拔必然更需要 5 脚"。**
> 对本项目的影响：**结论仍维持 5 脚**（已下单、且本项目是**裸板无定位板**，正落在"定位柱有价值"的情形里）—— 但理由要改成这一条，而不是"热插拔通用更优"。

**⑨ 轴座在底层** ✅ 正确，但 Kailh 官方规格书**只写 "Top View / 单面板 T=1.6"、未明说安装面**，判据来自"正装 MX 轴从顶面压入、针脚向下进底面轴座"的机械必然性。

**⑩ 「立创封装本来按底面画」无法确认** ⚠️：库文件焊盘实际标在 **Top layer**，不能据此断言其"按底面绘制"。⇒ **正确做法不变**（翻到底层），但**验证不能省**：翻面后必须以 CHERRY 顶视针位（左低右高）复核镜像方向。未找到嘉立创承认该封装画错的案例。

#### 加孔的具体做法（若最终仍用 C41430893 而不换 `KEYBOARD-SW`）

**用 `放置` → `挖槽区域` → 圆形**（与 OLED 安装孔同法，见第六节：圆形挖槽 ≤6.5mm 在 v1.9+ 会作为 NPTH 钻孔输出）。这三个孔是**纯机械孔，NPTH 不金属化**——轴体的中心导柱和定位柱都是塑料，不需要导电，也不需要在孔周围禁铜。

**坐标以键位中心为基准**：

| 孔 | 相对键位中心 | 绝对坐标（举 SW1 为例，中心 13.525, 36.575） |
|---|---|---|
| 中心孔 φ4.0 | (0, 0) | (13.525, 36.575) |
| 定位孔 φ1.7 | (−5.08, 0) | (8.445, 36.575) |
| 定位孔 φ1.7 | (+5.08, 0) | (18.605, 36.575) |

三孔共线、水平、左右对称（数据同上 KiCad 官方库）。**9 个键位 × 3 = 27 个孔**，建议做完一个键位后复制整组再按 19.05 的倍数平移，比逐个输坐标可靠。

> **孔在挖槽区域 ≠ 器件的一部分**，不随器件移动。所以**必须等键区位置最终确认、组合锁定之后再加孔**，否则后面整块平移键区时孔会留在原地。
>
> ⇒ **加孔排在阶段 E（安装孔）一起做**，与 OLED 的 4 个 NPTH 孔同一批，工具和方法完全相同。现在只要知道"必须加"，不用立刻动手。

#### φ4.0 中心孔是全板最大的通道杀手（布线时要记得）

9 个 φ4.0 孔 + 18 个 φ1.7 孔 + 加上工艺所需的孔到铜间距，是 `00-design-decisions.md` 第七节第 14 条说"键盘 PCB 布线比一般板难"的**具体原因**。特别是第二三排之间那条横带 —— 上下各有一排 φ4.0 中心孔向它侵入，剩余通道很窄。

这也再次说明双线束方案（A 束走左边缘+底边、B 束往右上）为什么重要：**它让 9 根按键线一根都不需要穿过这条横带**。等到布线阶段你会直接感受到这个设计的价值。

#### 摆完立刻做两件事

1. **框选 9 颗 → `布局` → `组合`**。组合后整块可平移/旋转而相对位置不变 —— 这正是"实测后要整块挪"需要的能力。
2. **锁定**（右侧「图层」栏点锁图标，或锁定组合），防手滑。

### C2. 摆放顺序与理由

**必须按此顺序**，因为前面的是"不可让"的刚性约束，后面的围着它们排：

| 顺序 | 摆什么 | 为什么在这个位置 | 刚性约束 |
|---|---|---|---|
| **1** | **9 个轴座** | 键区是全板最刚性的结构 | **19.05mm 键距**；1/4/4 三排；每键 φ4.0 中心孔 + 两侧 φ1.7 定位孔 |
| **2** | **U1 模块**（左上，天线朝左） | 天线净空是刚性的，且 A/B 两束线从它扇出 | 天线端朝左板边；**见下方 C2b：官方规则与"15mm 净空"的常见误解** |
| **3** | **OLED 母座 + 4 个安装孔** | 孔距刚性，且孔在板内会切断走线通道 | **孔距 21×21mm**，4 角孔；外形约 26mm 见方；排针居中在最下方 |
| **4** | **U3 + J5 + 喇叭占位框** | 验证 23:16 新定的"喇叭贴元件带背面"方案 | 喇叭 35×20mm，**必须偏板子右侧**远离天线 |
| **5** | Type-C / LDO / TVS / 阻容 / **TP1~TP5** | 大部分灵活，用来填空隙 | C8 必须最靠近 U3 的 pin6；**TP2/TP3 必须成对（见 C5）**；TP4 引线要短 |

**摆完轴座后建议锁定该层或组合起来**（`布局` → 组合），防止后续手滑移动。

### C2b. U1 天线净空：官方原文与「15mm」的误读（2026-08-08 01:00 查证）

> 信源：乐鑫官方《ESP32-S3 Hardware Design Guidelines》→ PCB Layout Design →
> "General Principles of PCB Layout for Modules (Positioning a Module on a Base Board)"
> `docs.espressif.com/projects/esp-hardware-design-guidelines/en/latest/esp32s3/pcb-layout-design.html`

#### ⚠️ 先澄清：网上流传的「15mm 净空」被普遍误引

大量二手资料（NextPCB、pcbsync、schemalyzer 等）写「天线四周各方向至少 15mm 净空，无铜、无器件、无走线」。**核对官方原文后，这是误读。** 官方原文是：

> "After the base board is placed in the end product, please consider the impact of the **housing** on the antenna during end-product design. Ensure that the PCB antenna on the base board also has a sufficiently large clearance area **inside the housing**. A clearance of **at least 15 mm** is recommended in all directions."

**15mm 讲的是「天线到外壳」的距离，不是「天线到板上铜/器件」的距离。** 本项目第一版是**裸板无外壳**（`00-design-decisions.md`），所以这条 15mm 目前**不构成约束**。

> 若第二版加外壳，15mm 会立刻变成外壳设计的硬约束 —— 届时按此条查，别到时又去翻二手资料。

#### 官方对板载净空的真实要求：三条，且都是**几何形状**要求而非数字

1. **首选：天线伸出基板之外**（悬在板框外）
   > "It is suggested to place the module's on-board PCB antenna **outside the base board**, and the feed point of the antenna close to the edge of the base board."

2. **若无法伸出：馈点尽量靠板边，并把天线两侧与下方的基板挖掉**
   > "If the antenna cannot extend beyond the board edge, the feed point should still be placed as close to the board edge as possible. Then **cut off the base board on both sides of the antenna and below it** to minimize the impact of the base board material on the PCB antenna."

3. **⛔ 明确禁止的一种做法：模块置于板中央、四周挖空**
   > "Note that the module should **not** be placed in the center of the board with clearance created by hollowing out on all four sides."

4. **净空区附近要有足够的地铜与密集地过孔**
   > "sufficient ground copper and dense ground vias should be placed on the base board near the antenna."

**⇒ 官方给的是图（"Keepout Zone for ESP32-S3 Module's Antenna (Antenna feed point on the Left)"），不是一个数字。** 具体尺寸必须从该图与官方推荐 land pattern 读取，本文不臆造数值 —— 这与 `README.md` 第七节"具体尺寸以官方资料为准"一致。

#### 本项目的选择：**方案 1（天线伸出板框外）**，理由与代价

本项目天线端朝左板边（已定），且**板宽有余量**（约 84.2mm，键区仅需 76.2mm）。因此**应优先让天线整体悬出左板框之外**——这是官方首选，效果最好，且**完全避免了在板内挖净空区**。

#### ⚠️ 更正（01:07）：「悬出」悬的是**模块自己**，不是我们的板

01:00 我写"悬出的天线段是一小片无支撑的 FR-4，运输易折""板宽增加约 6mm"。**两句都错**，更正如下：

**ESP32-S3-WROOM-1 是一块 18×25.5mm 的独立小板**，PCB 天线**长在模块自己身上**（屏蔽罩之外那一小段裸 FR-4）。所谓"天线悬出板框外"= 把模块摆在左板边、让**模块自己的天线端**探出去。

⇒ 由此两条纠正：
1. **我们的板不需要变宽，反而更省** —— 探出去的是模块，基板到天线那段根本不存在。是方案②（天线压在板内）才要额外板面积。
2. **不存在"基板伸出一根脆弱舌头"** —— 悬空的是模块自己的天线段（约 6mm×18mm），它由整块模块本体悬臂支撑，而模块是沿长边一整排焊盘焊死在基板上的。这是业界标准做法，不是特殊风险。

**需在 EDA 里确认一件事**：模块的天线端那条短边**是否有焊盘**。若无（预期如此，焊盘沿两长边 + 非天线端短边分布），则悬出 6mm 不损失任何焊盘，全部焊盘仍落在基板上。**用 land pattern 直接看，不要凭推测。**

**✅ 方案① 已采纳（2026-08-08 01:11 于子涵定）。**

#### 关于「天线是否贴着板子」的两次观察与最终澄清（01:12）

于子涵 01:11 报"3D 模型里天线在 MCU 上悬出、不贴板"，01:12 自行更正为"天线确实趴在板子上，刚才看错了"。**更正后的观察是对的**，且这个更正**不影响方案①的选择** —— 反而使①的必要性更明确。

**必须区分两个「贴着」，混淆会导致摆件判错：**

| | 是什么 | 本项目 |
|---|---|---|
| **模块 PCB ↔ 我们的基板**（垂直） | 表面贴装，垂直间隙仅约 **0.1~0.2mm** | **贴着**（无法改变，SMD 就是这样） |
| **天线投影 ↔ 基板板框**（横向） | 天线在俯视投影上是否落在板框内 | **由摆位决定 —— 这才是方案①要解决的** |

天线段本身是模块 PCB 上的一片铜箔，和模块基材是一体的，**不存在"抬离"这回事**。而 0.1~0.2mm 相对 2.4GHz 波长（自由空间 125mm）**等于零** —— 所以只要天线在俯视投影上压在我们的板子上方，它就必然被下方 FR-4 加载而失谐，高度差救不了。

**⇒ ①有效的唯一机制是「横向把天线移出板框投影」，与高度无关。** 摆件判据只有一条：

> **天线段在俯视投影上必须完全落在板框之外**；`Alt+M` 量出的悬出长度必须覆盖整段天线，探出一半无效。

**同时，01:11 那条"机械上无风险"的理由仍然成立**，但依据要换成正确的那条：悬空部分是模块自己的天线段，由整块模块本体经一整排长边焊盘悬臂支撑 —— 与"天线是否抬离基板"无关。

#### 三种做法对比（更正后）

| 做法 | RF 效果 | 真实代价 |
|---|---|---|
| **① 天线悬出板框外** ✅ | 官方首选，最好 | 模块天线段悬空 6mm（由模块本体悬臂支撑，业界常规）；**基板反而更省面积** |
| ② 天线在板内 + 两侧及下方挖空 | 次优 | 挖空吃掉左边缘通道 —— **A 束 4 根线正要走那里**，与双线束方案直接冲突 |
| ③ 天线压在板内、不挖空、仅不铺铜 | **官方未认可**，天线被 FR-4 加载而失谐（谐振下移 + 介质损耗） | 无几何代价，但 RF 性能无保障 |

**②与已定的双线束方案冲突** —— A 束（pin4~7 去第三排）沿**左边缘**下行，若在左边缘挖净空区这条通道就没了，而它是双线束"零交叉"的前提。**故②在本项目实质不可选。**

#### 若仍不愿悬出：③ 可作为有意识的降级，但要记录

**判据是"第一版需不需要 RF"。** 按 `00-design-decisions.md`：第一版**不需要联网弹琴**（第七节对策①原文），BLE HID / 小游戏属"预留"能力。⇒ **RF 不在第一版关键路径上**，③ 不会让板子不工作。

选 ③ 的话必须同时做到、并写入本文：
- 天线正下方**所有层**不铺铜、不走线、不放器件（这是③的底线，不可再退）
- 天线馈点仍尽量靠左板边（官方要求，与①②共通）
- 净空区周围放足够地铜 + 密集地过孔（官方原文要求）
- **明确记录"RF 性能未按官方首选方案设计"**，第二版若要用 BLE HID，需实测通信距离并可能改板

⇒ **建议：先按①摆**（代价已证明比 01:00 所述小得多）。若摆完发现①与元件带内其他器件冲突，再降级到③，而不是因为担心机械脆弱而放弃①。

#### 摆 U1 时的具体动作

1. **先放官方推荐 land pattern**（搜 C2913202 从立创库放，别自绘）
2. **旋转使天线端朝左**（`00-design-decisions.md` 已定），确认馈点在左侧
3. **横向位置**：让天线主体越过左板框线；**`Alt+M` 量悬出长度并记录**
4. **纵向位置**：在元件带内（y > 65.15），且给 pin1/pin2 的连接与 R6/C7 留出空间（`00-design-decisions.md` 标为"待实测的拥挤点"）
5. **量三项距离并记录**：天线 ↔ 喇叭占位框（永磁体，最敏感）、天线 ↔ Type-C/USB 走线、天线 ↔ J3 UART 排针
   > 官方明确点名：「the USB port, USB-to-serial chip, **UART signal lines (including traces, vias, test points, header pins)** must be as far away from the antenna as possible」——J3 是 UART 排针，正在此列。**这条此前文档未记录。**

### C2c. 电源段（J1 Type-C + D2 + U2 AMS1117 + C1~C4）的摆放依据（2026-08-08 01:14 查证）

> 信源：① AMS1117 官方 datasheet（SOT-223 版；请从所用器件厂商的官方页面获取并核对具体型号与版本）
> ② 乐鑫《ESP32-S3 Hardware Design Guidelines》→ PCB Layout Design → USB 节

#### 先回答"有没有手册可参考"：**LDO 没有，USB 有**

**AMS1117 datasheet 全文没有 PCB Layout 章节，也没有 reference layout 图。** 它只给电气参数、封装尺寸、以及散热计算公式。所以**不存在"照抄官方摆法"这条路** —— 摆位必须自己从热和回路两个物理约束推。这是查证结论，不是没查到。

**USB 部分乐鑫官方有明确要求**，见下 §4。

#### 1. 🔴 最重要的发现：datasheet 自相矛盾，而我们此前用的是宽松的那个数

`00-design-decisions.md` 第七节一直按 **1.2W** 作为 AMS1117 的耗散上限。但 datasheet 里有两个互相冲突的数：

| 出处 | 数值 |
|---|---|
| **Absolute Maximum Ratings 表** | **Maximum Power Dissipation (SOT-223) = 600 mW** |
| Electrical Characteristics 的 Note 2 | "guaranteed up to the maximum power dissipation (**1.2W**)" |

**这不是笔误，两个数各有前提，用 θJA 就能对上：**

同表给出 **θJA (SOT-223) = 150 °C/W**，而 TJ 保证上限 125°C。取环境 25°C：

- 允许温升 100°C ÷ 150 °C/W = **0.67W ≈ 600mW** ⇒ **600mW 是"裸封装、无额外铜箔"下的上限**
- 要达到 1.2W，需 θJA ≤ (125−25)/1.2 = **83 °C/W** ⇒ **1.2W 隐含"有良好散热铜箔"**

**⇒ 结论：铜箔面积不是"锦上添花"，它就是 600mW 与 1.2W 之间的那个差值。**

**核算本项目**：VIN=5V、VOUT=3.3V、压差 1.7V，最坏 500mA ⇒ **PD = 0.85W**。
- 超过 600mW（裸封装上限）⇒ **不铺铜必定过热**
- 低于 1.2W ⇒ 铺够铜可行
- 所需 θJA ≤ (125−25)/0.85 = **117 °C/W**（环境 30°C 则为 112）

从 150 降到 117 以下，幅度不大、可达（SOT-223 配足够铜箔可到 60~80 °C/W），**但必须真的做，不能默认**。

#### 2. 🔴 散热铜箔的网络是 `+3V3`，不是 `GND`

datasheet 原文：

> "The case of all devices in this series is **electrically connected to the output**."

**SOT-223 的大 tab = VOUT = `+3V3`**，不是 GND。这条极易搞错，`02-bom-footprints.md` 只写了"给大 tab 留足铜箔"未指明网络。

⇒ **两点后果：**
1. 散热铜箔是一片 **`+3V3` 铜**，必须与 GND 保持规则间距，**不能想当然当成地平面的一部分**
2. **这使阶段 G 的"顶层元件带局部铺 `+3V3`"从「可选」变为「必需」** —— 它同时是散热器和供电铜箔，一举两得。原方案里那句"（可选）"应改为必需，理由从"降低供电阻抗"升级为"LDO 唯一散热路径"

**摆位要求**：U2 周围（尤其 tab 一侧）留出连续的顶层 `+3V3` 铜面，**不要被走线切碎**。元件带正面本来较空（`00-design-decisions.md`），这是可用条件。

#### 3. 电容摆位：datasheet 给的是电气要求，几何要自己推

datasheet 原文要点：
- 输入：**"A 10μF tantalum on the input is a suitable input bypassing"**
- 输出：**"requires a capacitor from VOUT to GND to provide compensation feedback to the internal gain stage. This is to ensure stability"**
- **"It is important that the ESR for this capacitor does not exceed 0.5 Ω"**

⚠️ **注意输出电容的性质**：它不只是滤波，**是内部环路补偿的一部分** —— 少了或离得远，LDO 可能自激振荡。所以 **C3（22µF）必须紧贴 U2 的 VOUT 脚**，优先级高于 C1。

#### SOT-223 引脚定义（先确定这个，否则"靠近输出"没有意义）

| 脚 | 名称 | 说明 |
|---|---|---|
| **1** | **GND**（ADJ 版为 ADJ） | 三条腿中最靠边的一条 |
| **2** | **VOUT** | 三条腿中间那条 |
| **3** | VIN | 另一侧边缘那条 |
| **4** | **tab（大散热片）= VOUT** | 与 pin2 **内部相连、同一网络** |

**⇒ pin2 与 pin4 电气上是同一个点**（datasheet: "The case of all devices in this series is electrically connected to the output"）。所以"靠近 pin4"和"靠近 pin2"在**网络上**没区别 —— 但在**几何上**差别很大，见下。

#### 🔴 C3/C4 应靠 **pin2**，不是 pin4（tab）

**理由：去耦要最小化的是「电流回路面积」，而回路必须回到 pin1(GND)。**

电流路径是：`VOUT → 电容 → GND → 回到 pin1`。而 **pin1(GND) 与 pin2(VOUT) 是相邻的两条腿**，所以电容摆在 pin1–pin2 这一侧，回路可以做到极小。

若摆在 tab(pin4) 那一侧：tab 在封装**另一面**，电流出了 tab 经电容到 GND 后，还得**绕过整个封装本体**才能回到 pin1 —— 回路面积大出好几倍，而回路面积正是决定去耦效果和环路稳定性的量（同 `00-design-decisions.md` 第七节第 7 条）。

**另一个理由**：tab 要留作**连续的散热铜面**（见本节 §2）。往那儿堆电容会把铜面切碎，散热和去耦两头都不讨好。

⇒ **分工明确：pin2 侧放电容（管电气），tab 侧留铜面（管散热）。**

#### 摆放优先级

| 顺序 | 器件 | 位置 | 理由 |
|---|---|---|---|
| 1 | **C4 0.1µF** | **最贴 pin2**，另一端尽量直接连 pin1(GND) | 高频段寄生电感最小者应离得最近 |
| 2 | **C3 22µF** | 紧邻 C4，仍在 pin1/pin2 一侧 | 环路补偿 + 储能，容量大但寄生电感也大 |
| 3 | C2 0.1µF | 最贴 pin3(VIN) | 输入高频 |
| 4 | C1 22µF | 紧邻 C2 | 输入储能 |

> **注意顺序与 01:14 版相反**：先前写"C3 第一、C4 第二"，按"小容量更贴芯片"的通行原则应为 **C4 先、C3 后**。小容量电容以较低寄生电感处理高频快速事件，必须离芯片最近。已更正。

> **留档（不阻塞，第二版再看）**：AMS1117 系列已知有"输出用极低 ESR 的 MLCC 时稳定性变差"的现场反馈（eevblog / r/PrintedCircuitBoard 讨论，建议改钽电容或串 0.1~0.5Ω）。**datasheet 只规定 ESR 上限 0.5Ω，未规定下限**，故本项目 C3 用 MLCC 不违反手册。若实测 3V3 轨有异常振荡再处理。**这是二手来源，标记为待验证。**

#### 4. USB 段：乐鑫官方有明确要求（原文）

> - "Reserve space for resistors and capacitors on the USB traces close to the chip side."
> - "Use **differential pairs** and route them **in parallel at equal lengths**. Maintain a differential pair impedance of **90 Ω ±10%**."
> - "USB differential traces should **minimize via transitions**... If vias are necessary, add a pair of ground return vias at each transition point."
> - "Ensure there is a **continuous reference layer** (a ground layer is recommended) beneath the USB traces."
> - "**Surround the USB traces with ground copper.**"

**⇒ 对本项目（`IO19`/`IO20`）的可执行版本**：两根等长并行、间距一致、**下方底层 GND 必须连续（不能跨越 GND 的裂缝）**、两侧包地、尽量不打过孔。

> 90Ω 差分阻抗在两层板上依赖板厚与线宽，第一版**不做阻抗控制**（USB Full-Speed 12Mbps 对阻抗宽容）。**记录为"未按 90Ω 设计"**，不假装满足。

#### 5. ⚠️ 一个尚未解决的布局冲突：USB 要远离天线，但右侧已被喇叭占了

乐鑫官方点名（原文见 C2b §5）：

> "the **USB port**, USB-to-serial chip, UART signal lines... must be as far away from the antenna as possible"

而本项目：**天线在左** ⇒ USB 应偏右。但 `00-design-decisions.md` 已定 **喇叭（含永磁体）也必须偏右**，理由同样是远离天线。**元件带内左右两侧的"远离天线"资源被两个器件争用。**

**这个冲突此前文档未识别，需要在摆件时决策。** 三个可能方向（未定，待摆件时按实测空间选）：

| 方案 | 说明 | 代价 |
|---|---|---|
| A. Type-C 放**上边缘中部偏右**，喇叭贴**背面更右** | 二者一正一背、投影可部分重叠 | 需确认 Type-C 焊盘与喇叭占位框不冲突 |
| B. Type-C 放**右侧板边** | USB 离天线最远 | 插线方向从侧面出，可能碍手 |
| C. Type-C 偏右、喇叭略往中间移 | 折中 | 喇叭离天线变近，与已定约束冲突，**优先级最低** |

**判据顺序**：喇叭离天线的距离优先（永磁体对 RF 的影响比 USB 走线大且不可屏蔽），USB 次之；且 **USB 是可以靠包地和缩短走线缓解的，喇叭的磁体不能**。

### C2d. 元件带重排：I2S 交叉、喇叭位置、U3 藏屏下（2026-08-08 02:10~02:13 讨论）

#### 1. I2S 四条线交叉 —— 改引脚映射解决，**不打过孔**

**ESP32-S3 的 I2S 走 GPIO Matrix，BCLK / LRCLK / SDATA 可映射到几乎任意 GPIO**；CTRL 是普通 GPIO 输出。⇒ 四根线哪根接哪个脚**是软件一行配置**。

解法：NS4168 侧脚序固定（pin1 CTRL、pin2 LRCLK、pin3 BCLK、pin4 SDATA），按**模块侧四个焊盘的物理顺序**与之同序配对 ⇒ 四线单调、零交叉。

这与 A 束已用过的手法相同（`00-design-decisions.md`：pin4→SW7、pin5→SW6…SW 编号反排以保单调），也正是该文第 86 行原则的应用：

> 引脚号是**逻辑建议值**。PCB 布局时若某 IO 走不通就换 —— 软件改引脚号是一行代码，布线走不通是物理问题。

**操作**：只把网络标签（`I2S_BCLK` 等）挪到别的模块引脚上，**网络名一字不改** —— 这正是当初按用途命名而非 `IOxx` 命名的收益。改完固件同步改 `i2s_config` 的三个 pin 号。
> 已确认 IO12/13/14/21 均不在禁用名单（IO35/36/37 被 Octal PSRAM 占用；IO3/45/46 为绑带脚）。

#### 2. ⭐ 关键澄清：**喇叭位置与 U3 位置是解耦的**

此前文档把"U3 + J5 + SPK1"当成必须同处的三件套，**这是不准确的**。看连接方式：

| 连接 | 形式 | 对几何的约束 |
|---|---|---|
| U3 VoP/VoN → **J5** | **PCB 走线** | ⚠️ 必须短而宽（NS4168 手册 10.6）⇒ **J5 必须紧邻 U3** |
| J5 → **SPK1** | **120mm 红白导线**（C49247026 自带） | **无 PCB 走线** ⇒ 喇叭位置自由 |

⇒ **只有 J5 必须贴 U3；喇叭靠导线连接，位置完全自由。** 喇叭的真实约束只有四条：
1. 背面**平整**区域（双面胶唯一可靠落点）
2. 35×20mm 空间
3. 离天线足够远
4. **不压住任何需要操作的东西**（见 §4）

#### 3. 喇叭挪到 U1 与 USB 之间：**物理判断成立，采纳**

于子涵 02:10 提出，并给出理由"天线已伸出板外，板内隔 30~40mm 影响应该不大"。**该判断正确**，两条依据：

- **2.4GHz 波长 122mm，反应近场边界 ≈ λ/2π ≈ 19.5mm。** 30~40mm 已在边界之外，导体扰动比 20mm 内小一个量级。
- **更强的一条**：天线伸出板外、喇叭在模块另一侧，**模块自身的屏蔽罩与地平面挡在中间**。那 30~40mm 不是"空气直线距离"，是"绕过一个屏蔽金属盒的距离"。

**修正 `00-design-decisions.md` 的一处外推错误**：该文"喇叭必须偏板子右侧"的依据是"永磁体在净空区内会显著劣化 RF" —— **该句在净空区内成立，但被外推成了"越右越好"**。净空区之外，30mm 与 50mm 的差别远小于走线绕行的代价。

**换来的收益是实的**：原方案 I2S 四线需穿过 USB→电源走线与屏幕走线。危害具体 —— I2S 的 BCLK 是 MHz 级方波、边沿谐波达数十 MHz，与 USB 差分对并行即互相耦合；而乐鑫官方要求 USB 走线包地、下方参考层连续，横穿会破坏之。

#### 4. 🔴 U3+J5 藏 OLED 下方：**高度可行，但有两个硬冲突**

于子涵 02:13 提出。**高度判断正确**：OLED 用 8mm 尼龙柱抬起，而 U3(ESOP-8) 约 1.27mm、C9/C10(1206) 1.6mm、J5 卧贴约 2mm —— **余量充足**。

但有两点必须先解决：

**① ⛔ 喇叭不能贴在 OLED 正下方的背面 —— 4 个螺丝头在那里**

OLED 的 4 个 NPTH 孔用尼龙柱固定：螺丝**从板子背面穿上来**进入柱子。⇒ **背面那 4 处会有凸起的螺丝头**，且装配时需要从背面拧。

而喇叭是**双面胶贴背面**，需要平整面。若贴在 21×21mm 孔阵区域上：
- 机械上：架在 4 个螺丝头上，四角悬空 —— 这正是当初否决"喇叭贴键区背面"的同一个失效模式（`00-design-decisions.md`：轴座凸起 1.8mm 使胶无法贴合）
- 操作上：胶贴上去就**无法再拧那 4 颗螺丝**，屏也拆不下来

⇒ **喇叭必须与 OLED 的 21×21 孔阵在投影上错开。** 结合 §2（喇叭位置自由），把喇叭放在 **U1 与 OLED 之间**的背面即可 —— 该区正面只有贴片件、背面平整。

**② ⚠️ J5 的插拔通路会被 OLED 遮住**

J5 是**卧贴**座，插头需**水平**推入。若 J5 在 OLED 正下方，8mm 高度虽够手指伸不进去。两条出路：
- **装配顺序约束**：先插喇叭线、后装 OLED。可行但**每次换喇叭都要拆屏**，且第一次上电流程（`05` 第十二节：先不插模块、验电压后再插 OLED、最后插喇叭）**顺序会冲突**
- **更好：J5 放在 OLED 投影之外**，只把 U3 和 C8/C9/C10 藏屏下

⇒ **推荐方案**：U3 + C8/C9/C10 藏 OLED 下方（它们焊上后不需再操作），**J5 移出 OLED 投影范围**、仍紧邻 U3（§2 只要求 J5 贴 U3，不要求在屏下）。

**③ 顺带确认（不阻塞）**：OLED 下方还要走 J2 母座的 I2C 线，与 U3 及 4 个孔竞争空间。摆件时一并量。

#### 5. ⚠️ 建议先定元件带整体分区，再逐件摆

以上三项改动等于**重排元件带左右顺序**。原顺序（U1 左 / OLED 中 / 喇叭+U3 右）建立在"喇叭必须远离天线"这个已被放宽的前提上。

**建议**：先用丝印框在 EDA 里划一遍从左到右的分区，把全部器件放进去看整体，再逐件精摆。需同时安置：U1、喇叭(背面)、U3+C8/C9/C10、J5、OLED+4 孔、Type-C、U2+C1~C4、D1/D2/R1~R3、SW10/11、TP1~TP5。

⚠️ **别忘 U2 的散热铜面**：tab 需一片**连续** `+3V3` 铜（0.85W 唯一散热路径，见 C2c），该铜面不能被别人的走线切碎 —— 这是分区时必须预留的面积，容易被忽略。

### C3. 喇叭占位技巧

SPK1 是**板外器件**（无封装、双面胶贴背面），PCB 上没有它的实体。做法：

在**机械层或丝印层画一个 35 × 20mm 的矩形框**作占位。好处：
- 直观看到它与天线净空区、与 U1 的距离
- 板框高度需要多少一眼可见
- 以后贴双面胶时有丝印对准，不用拿尺子量

### C4. 摆件时必须用 `Alt+M` 实测的五项

这些是 `00-design-decisions.md` 里标为"几何推断、待验证"的项，**摆件阶段就能验证，不需要等实物**：

1. **底边通道宽度** ≥ 8mm（现在只需容纳 A 束 4 根线，喇叭和 J5 已移走）
2. **喇叭占位框到天线 keepout 的距离** —— 喇叭含永磁体，进净空区会显著劣化 WiFi/BLE
3. **OLED 的 26mm 纵向是否侵入第二排键位**；4 个孔有没有压到别的器件
4. **A 束 4 根线的路径**：从 U1 左侧扇出沿左边缘/底边到第三排，中途有没有被键位孔堵死
5. **J5 卧贴座的插拔方向**：正前方不能被元件或板框挡住

### C5. 测试点 TP1~TP5 的摆位（不是"随便填空隙"）

> 2026-08-08 00:04 补。此前文档只记 TP1~TP3 且笼统归入"灵活件"，导致漏掉示波器测法对几何的约束。

**全板 5 个测试点，网络为：TP1=`+5V`、TP2=`+3V3`、TP3=`GND`、TP4=`EN`、TP5=`BOOT`。全板只有 TP3 一个 GND 点。**

#### 铁律：TP1 — TP3 — TP2 排成一排，TP3 在中间，间距 2~3mm

TP2 要做的不只是量万用表平均值，而是用**示波器 DC 耦合**捕获最大音量、无线活动和启动瞬间的绝对最低电压；AC 耦合只用于放大观察小纹波。功能下限为不得跌破 3.0V，完整测试方法见 `07-hardware-test-guide.md`。这项测量对几何有硬约束：

**探头的信号尖 + 地夹围出的回路面积就是天线面积。** 若 TP2 与 TP3 相隔几厘米，地线绕出的大环会耦合进环境噪声——而 NS4168 以 430kHz 开关、板上还有 WiFi 发射，正是最强的干扰源。要观测的凹坑本身只有几十毫伏量级，很容易被这种测量噪声完全淹没或伪造出来。**后果不是"测不准"，而是把测量误差误判成电源设计缺陷**，去查 LDO、加电容、甚至第二版换 DC-DC，全是白做。

所以 TP3 摆在 TP1 和 TP2 中间，三点一排、彼此 2~3mm。这样一个探头地夹不动，就能挨着量两路电源，且两次的回路都最小。

⚠️ **不增加第二个 GND 测试点**（00:04 决策）。理由：唯一需要小回路的是 TP2，靠上面的相邻摆位已经解决；而底层是整片 GND 铺铜，真需要额外接地时任何过孔、任何器件的 GND 焊盘都可当地点。多开焊盘只是多占面积。

#### TP4 (`EN`) 的特殊约束：引线必须短

EN 是 **10kΩ 上拉 + 1µF 到地**的高阻节点，节点阻抗高意味着**容易被容性负载和串入噪声影响**。测试焊盘本身就是一小片铜，接它的走线也有电容——挂在 EN 上等于给 R6/C7 的延时网络并了个额外电容，理论上会拖慢上电延时。1µF 面前这点寄生电容可以忽略，**但前提是引线短**；从板子另一头拉一根长线过来就不好说了。

因此 TP4 摆在 **R6 / C7 / SW11 附近**，短线接入，别为了"好探针"把它挪到板边。TP5 (`BOOT`) 同理靠近 R7 / SW10，不过 BOOT 只在启动瞬间被采样、平时是静态高电平，宽松得多。

#### ⛔ TP4 / TP5 绝不能紧邻 TP1(`+5V`)

**EN 和 IO0 都是 ESP32-S3 的 IO 引脚，绝对最大额定约 3.6V；TP1 上是 5V。** 探针打滑或焊渣搭桥把 5V 引到 EN / IO0，会直接损坏模块，而且是不可逆的。

这一条与"TP4 靠 R6/C7、TP5 靠 R7/SW10"天然一致——R6/R7 都在第二页 MCU 区，TP1 在第一页电源区，本来就隔着。**但摆件时要主动确认这个间隔真的存在**，不要在收板框、填空隙的时候把 TP4/TP5 顺手挪到 TP1 旁边的空位上。5V 点和 3.6V 上限的点物理分开，是一条不需要权衡的设计纪律。

#### 丝印

TP1~TP5 **必须标网络名，不能只标位号**。第九节丝印清单里已列。理由很实际：调试时你手拿着探针、眼睛在示波器上，靠数位号找点必然会错，而 **TP1 是 +5V、TP2 是 +3V3，探错的直接后果是把 5V 灌进 3.3V 轨**。写 `+5V` / `+3V3` / `GND` / `EN` / `BOOT` 五个字，零成本。

### C6. 摆完后定最终板框

量出实际占用后，把临时板框收到实际尺寸。**板高 = 元件带（≥22mm）+ 三排键位（57.15mm 刚性）+ 底边通道（≥8mm）**，参考下限约 87mm，**但以实测为准，不是设计目标值**。

⚠️ **收板框时只动上边和右边，左下角必须不动**（见 B5）。画布原点设在临时板框左下角，9 个轴座的绝对坐标全部以它为基准；一旦左下角移位，所有已摆器件的坐标含义就变了。90×100 收到约 (70~80)×87 正好是往上、往右收，天然满足。

**倒圆角**：选中**完整闭合**的板框 → **右键 → `添加圆角`**；或选矩形板框 → 右侧属性 → 圆角半径。建议 **R2~3mm**（直角尖端运输易磕碎、也会割手）。

---

## 五、阶段 D：禁铺区（天线净空）

⚠️ **这一步必须在铺铜之前做。** 铺铜是自动填充的，会一直填到板边**包括天线下方**，盖进净空区会让 WiFi/BLE 直接不可用。

**顶部菜单 `放置` → `禁止区域` → 矩形** → 在属性里**勾选「铺铜」**

### 官方明确的概念区分

| 功能 | 作用 | 本项目用途 |
|---|---|---|
| **禁止区域** | 限制在该区域内放置某类元素（勾选"铺铜"后铺铜会挖空） | **天线净空区** |
| **挖槽区域** | 把**板材物理挖穿** | **OLED 的 4 个 NPTH 安装孔**（见下） |

**两者完全不同，不要混用。**

天线净空区范围**必须按乐鑫 ESP32-S3-WROOM-1 官方推荐封装/keepout 图确定**，不凭文字估算。该区内：不铺铜、不走线、不放元件（含喇叭）。

---

## 六、阶段 E：安装孔

### OLED 的 4 个安装孔（非金属化 NPTH）

**顶部菜单 `放置` → `挖槽区域` → 圆形**

> 官方说明：圆形挖槽 ≤6.5mm 在 **v1.9+** 版本 Gerber 输出中会作为 **NPTH 钻孔**处理。这正是我们需要的——本项目用**尼龙柱**，孔不需要金属化。
>
> 对比：若要金属化孔（PTH），应改用 `放置` → `焊盘`（快捷键 `P`）。

**本项目为什么用 NPTH + 尼龙柱**（详见 `00-design-decisions.md`）：尼龙不导电 ⇒ 安装孔周围**不必禁铜**，底层 GND 平面保持完整 —— 两层板上完整地平面是稀缺资源，这是实打实的收益。

**孔径待实物卡尺确认**（决定 M2 还是 M3 尼龙柱）。

### 键区的 27 个机械孔（9 键位 × 3）

轴座封装**只含 2 个 φ3.0 针脚孔**，轴体所需的 **1× φ4.0 中心导柱孔 + 2× φ1.7 定位柱孔必须自己加**（依据与坐标见第四节 C1b）。同为 `放置` → `挖槽区域` → 圆形、NPTH 不金属化。

**为什么排在本阶段而不是摆件时**：挖槽区域不随器件移动，键区位置必须先最终确认。

### ⚠️ 本阶段加完孔后立刻做的三项检查

1. **手量 `Alt+M`**：左侧 φ3.0 针脚孔 → 左侧 φ1.7 定位孔的边到边距离，预期 **0.49mm**，嘉立创下限 0.45mm（余量仅 0.04mm，是全板最紧的间隙）。详见 C1b。
2. **跑 DRC**（孔到孔间距设 0.45mm），并与第 1 步比对，**借此判定 DRC 是否覆盖挖槽区域** —— 手量 0.49 而 DRC 沉默 ⇒ 不覆盖 ⇒ 今后挖槽区域间距一律手量。**该结论同时决定 OLED 4 个安装孔能否依赖 DRC。**
3. **数总数**：键区应共 **27 个新增挖槽孔**（9×3）。漏一个 ⇒ 那颗轴体装不上，**DRC 与 3D 预览均不报**。

---

## 七、阶段 F：布线

**顶部菜单 `布线` → `单路布线`**，快捷键 **`Alt+W`**

- 布线在**当前活跃图层**进行 → 先切层再布
- 左键加拐点，右键取消

### 布线顺序建议

1. **电源优先**：`+5V` / `+3V3` —— 用已建的 `power` 规则（0.5mm）
2. **关键信号**：U3 的 VoP/VoN 到 J5（手册 10.6 节要求**尽量短、尽量宽、远离敏感信号线**）
3. **其余信号**：I2C / I2S / 按键线

> **按键线的分层策略（00:27 定，依据"轴座在底层"）**：
> - 轴座 pin2 (`GND`) —— **不布线**，底层铺铜直接连上（焊盘就在 GND 平面同层）
> - 轴座 pin1 (`KEY_xx`) —— 焊盘处打过孔上顶层，**在顶层**走到 U1
>
> 理由：键区顶层空无一物（所有器件都在元件带），是白送的布线空间；若在底层走这 9 根线，会把整片 GND 平面切开，而键区底层已被 φ4.0 + φ1.7 孔切碎，回流被迫绕远。代价仅 9 个过孔。
4. GND 大部分交给底层铺铜，不用逐根布

### 本项目布线的特殊约束

- **VoP / VoN**：走线短而宽，远离 I2C/I2S 等敏感线。**两根都不接地**
- **C8 (1µF)** 到 U3 pin6 的回路要最短 —— 高频去耦靠的是小回路面积
- **A 束 4 根线**走左边缘/底边到第三排，**不得侵入天线净空区**
- 键区被 φ4.0 + φ1.7 孔切碎，通道稀缺；第二三排之间的横带是全板最紧张的资源

---

## 八、阶段 G：铺铜

**顶部菜单 `放置` → `铺铜区域`**，快捷键 **`E`**

左键开始画 → 再次左键确认 → 右键取消。画完弹窗可设**层、网络、其他属性**。

> 官方说明：铺铜区可**直接画在板框外部**，不需要精确沿板框描边 —— 软件会自动裁到板框内。

### 本项目铺铜方案

| 层 | 铺什么 | 理由 |
|---|---|---|
| **底层** | **整片 `GND`** | 回流路径完整性。**两层板上连续 GND 平面是最稀缺的资源** |
| 顶层 | **元件带区域局部 `+3V3`**（可选） | 该区无键位孔、较空；供电阻抗更低且**不切开底层 GND** |
| 底层 | ⛔ **绝不再铺 `+3V3`** | 会把 GND 平面撕成几块，回流被迫绕路 —— 为省 20mV 压降损失整板回流完整性，净亏 |

### 关键概念：铺铜是为了回流，不是为了载流

1oz 铜（35µm）方阻约 0.48mΩ/方。0.5mm 宽 × 50mm 长 ≈ 48mΩ，0.5A 时压降仅 **24mV**（占 3.3V 的 0.7%），而 NS4168 供电下限 3.0V 尚有 235mV 余量 —— **载流和压降都不是问题**。

铺铜的真正价值是**回流路径**：电流必须成回路，GND 若只是细线则回路面积大 = 环形天线（既辐射也接收干扰）；整片 GND 平面让回流自动走正下方最短路径，回路面积最小。NS4168 以 **430kHz** 开关、边沿谐波达数十 MHz，对回路面积极敏感 —— 手册 10.6 节「地线最好采用星形接法」本质就是在管回流。

### 孤岛铜（死铜）必须移除

铺铜被走线和孔切割后可能产生与主体不连通的"孤岛"，即浮空金属，面积一大即成寄生天线。

**选中铺铜区 → 右侧属性面板「保留孤岛」→ 设为「否」** → 按 **`Shift+B`** 重建铺铜。

---

## 九、阶段 H：检查与收尾

### DRC

**顶部菜单 `设计` → `检查DRC`** → 结果在**底部 DRC 面板**
- 单击错误项 = 定位；双击 = 放大定位
- 官方未给固定快捷键
- **目标：0 error；剩余 warning 逐条解释并留痕**（项目规矩，见 `00-design-decisions.md` 第七节第 17 条）

### 3D 预览

**顶部菜单 `视图` → `3D预览`**，或顶部工具栏 3D 图标

> 若器件未绑定 3D 模型，先用 `工具` → `3D模型管理器` 处理。

**本项目 3D 预览要专门看**：OLED 与键帽的高度关系、喇叭占位与 U1 的空间关系、J5 的插拔空间。

### 丝印

选中丝印对象 → 右侧属性面板修改。新增丝印：放置文字后把图层改为丝印层。

**本项目必须加的丝印**：
- ⚠️ **OLED 母座 4 个脚的网络名**（`GND` / `+3V3` / `I2C_SCL` / `I2C_SDA`）—— 市面模块排针顺序至少有两类，**VCC/GND 对调有损坏模块的风险**，插屏前必须逐脚核对
- 喇叭占位框（35×20mm）
- **TP1~TP5 必须标网络名**（`+5V` / `+3V3` / `GND` / `EN` / `BOOT`），**不能只标位号**——调试时手拿探针眼看示波器，数位号必然出错，而 TP1(+5V) 探成 TP2(+3V3) 就是把 5V 灌进 3.3V 轨
- J5 的 1/2 脚极性标注（红/白线对应关系，实测后补）

**工艺下限**：丝印字符线宽 ≥0.15mm，字高 ≥1mm（嘉立创官方要求）。

---

## 十、阶段 I：输出

| 输出物 | 路径 |
|---|---|
| **Gerber** | `导出` → `PCB制板文件(Gerber)`（可点"自定义配置"） |
| BOM | `导出` → `物料清单(BOM)` |
| 坐标文件 | `导出` → `坐标文件`（SMT 贴片需要） |
| PDF | `导出` → `PDF/图片` |
| 3D STEP | 先进 3D 预览 → `导出` → `3D文件` |
| **一键下单** | `下单` → `PCB下单` → 确认 |

> 一键下单使用**默认导出参数**。需自定义 Gerber 时应先导出再到 jlc.com 上传。

---

## 十一、下单前必须完成的实物验证清单

以下项**不阻塞 PCB 绘制，但必须在下单前闭环**。汇总自 `README.md` 第五节与 `00-design-decisions.md`：

| # | 验证项 | 方法 | 若不通过怎么办 |
|---|---|---|---|
| 1 | **U3 的 EP ↔ pin7(GND) 通断** | 万用表蜂鸣档 | 通=内部即GND（最优）；不通=浮空（接GND仍无害）。仅留档 |
| 2 | **J5 的 3/4 脚 ↔ 1/2 脚通断** | 测 3↔1、3↔2、4↔1、4↔2 **四次**，插喇叭前做 | **任一响 → 立即把 3/4 脚改回 NC**。接 GND 会短路桥接输出，现象是没声/断续，极易误查成 I2S 或固件问题 |
| 3 | **喇叭插头与 J5 实物试插** | 散买各 1 件，确认能插到底、不过紧、不松脱、两触点导通无短路 | 换 J5，不硬插不削胶壳；喇叭本体可保留 |
| 4 | **OLED 孔径** | 卡尺 | 决定 M2 还是 M3 尼龙柱 |
| 5 | **OLED 母座塑高 + 叠高** | 卡尺量母座塑高、屏 PCB 厚、玻璃组件厚 | 决定尼龙柱长度（现策略：8mm 柱 + 0.5mm 垫片，另买相邻长度试装） |
| 6 | **OLED 是否自带 I2C 上拉** | 量 SDA↔VCC、SCL↔VCC | 自带则不焊 R4/R5 |
| 7 | **OLED 排针顺序** | 核对实物 | ⚠️ **VCC/GND 对调会损坏模块**。顺序不符则用 4 根独立跳线转接，不可直插或反装 |
| 8 | **喇叭红/白线对应 J5 哪脚** | 实测 | 单喇叭相位接反通常不损坏，但文档与丝印必须统一 |
| 9 | **SW10/11 轻触开关分组** | 蜂鸣档：不按时两脚响=同组 | 找不同组的一对；**推荐四脚全接**以免疫分组记错 |
| 10 | **C15008 库存与库属性** | 嘉立创下单页实时查 | 若变成扩展库收费，重新评估 |
| 11 | ~~D2 SMF5.0A 立创物料编号~~ | **✅ 已补全（23:54）：C151296**（Littelfuse SMF5.0A，**SOD-123FL**） | 放置时搜编号取官方封装；注意是 SOD-123**FL** 不是 SOD-123 |
| 12 | **NS4168 / J5 可贴库存** | 下单页实时查 | 商城散买与贴装库存是两条独立供应链 |

---

## 十二、焊接与调试阶段的注意事项（提前知道，别到时踩）

### 分工

| 交工厂贴片（SMT） | 自己手焊 |
|---|---|
| 全部 0603 / 0805 / 1206 阻容（含 R8、C9/C10）· NS4168 · J5（试插通过后）· D1/D2 | Type-C · AMS1117(SOT-223) · 9 个轴座 · J2/J3/J4 · SW10/11 轻触开关 · SPK1 喇叭本体（双面胶） |

> **C9/C10 改贴片 MLCC 后，手焊清单里已无任何有极性器件** —— 焊反出事的机会已被整体消灭。

### 上电顺序（第一次通电）

> **测试点共 5 个**：TP1=`+5V`、TP2=`+3V3`、TP3=`GND`、**TP4=`EN`**、**TP5=`BOOT`**（00:04 补记，此前文档只写了 TP1~TP3）。

1. **先不插任何模块**（不插 OLED、不插喇叭），只上电
2. 量 TP1(+5V)、TP2(+3V3)，以 TP3(GND) 为参考 —— TP2 应稳定接近 3.3V；若高于 3.6V、低于 3.0V或持续跳变，立即断电。严格的静态验收范围须在锁定 C6186 实际厂商、完整 MPN、datasheet 和测试负载后确定，详见 `07-hardware-test-guide.md`
3. 摸 U2(AMS1117) 是否异常发热
4. **量 TP4(EN)**：应为 ≈3.3V。**若 EN 停在 0V，芯片被永久按在复位态，板子完全不启动**——这时优先怀疑 SW11 轻触开关的两根线误接到了同一组脚上（同组出厂即内部短接 ⇒ EN 直接短到 GND）。这是 `02-bom-footprints.md` 第三节点出的具体坑，现象是"LED 亮但不启动"，极易误查成 LDO 或焊接问题。**推荐四脚全接可完全免疫此错**
5. **量 TP5(BOOT)**：不按 SW10 时应为 ≈3.3V（R7 上拉）。若为 0V，同样查 SW10 的分组接错。**BOOT 若在上电瞬间为低，芯片会进下载模式而不是运行固件**——现象是"上电没反应"，但和 EN 故障是完全不同的原因，靠 TP4/TP5 两个点可以一次分开
6. 按一下 SW11(RESET) 和 SW10(BOOT)，看 TP4/TP5 是否被拉到 0V 再回弹 —— 顺便验证开关分组接对了
7. 电压正常后再插 OLED（**插前逐脚核对丝印网络名**）
8. 最后插喇叭

> **TP4/TP5 的排查价值**：EN 卡低和 BOOT 卡低都表现为"上电没反应"，但成因完全不同（一个是芯片没脱离复位，一个是进了下载模式）。没有这两个测试点，你只能拆着测模块引脚；有了它们，两次万用表读数就能定位。这也是为什么它们该摆在 R6/C7/SW11 和 R7/SW10 附近而不是板边——见第四节 C5。

### 第一版固件必须做的两件事

1. **CTRL(IO21) 在初始化最早期设为输出低** —— 虽然 R8 已提供硬件默认静音，软件也应尽早接管
2. **限制最大音量** —— 3.3V 供电下输出功率已受限，且要观察 3V3 轨跌压
3. **单声道数据写进右声道时隙** —— 因为 CTRL 高电平选的是右声道

### +3V3 轨的实测判据（不只看直流值）

完整步骤见 `07-hardware-test-guide.md`：
- 用示波器 **DC 耦合**捕获最大音量、无线活动和启动瞬间的绝对最低电压；功能下限为不得跌破 3.0V
- 再切 **AC 耦合**放大观察小纹波；AC 耦合画面不能单独判断绝对最低电压
- 同时记录 U2 温升，不只用手摸作唯一判据
- 最大音量、Wi-Fi/BLE 活动应先分别测试，再做组合峰值测试；出现复位或明显跌压时立即降负载排查

---

## 十三、给新会话的工作方式要求

沿用 `README.md` 第七节，并针对 PCB 阶段补充：

1. **涉及具体菜单路径**：以本文核实过的路径为准。若软件实际不符，说明版本更新 —— **回来更新本文，不要口头绕过**。
2. **涉及几何结论**：必须区分「已在 EDA 用 `Alt+M` 实测」与「按尺寸推断」，后者明确标为推断。
3. **审查截图**：把「截图中可辨认的内容」与「根据手册/文档确认的事实」分开；分辨率不足时不硬猜，直接要求放大重拍。
4. **不得从 `03-corrections-history.md` 恢复旧方案。**
5. **定稿内容（三页原理图）若需改动**：先说明是哪一条决策、依据是什么，不直接改。
6. **一次性新增大量规则时，主动提出核查，不等用户质疑。** 2026-08-08 凌晨首夜暴露了 6 处错误 + 4 处过度断言（见 `03-corrections-history.md` 记录 Y），全部是在密集新增规则后被发现的。
7. **"是金属的""看起来像"这类描述不足以判定电气属性。** 唯一判据是右侧属性面板有无网络字段 —— 直接问这个。
8. **区分"该验证"与"现在能验证"**：把验证动作挂到正确的阶段上，前置条件不存在时不要求执行。

---

## 十四、下一次开工的起点（2026-08-08 02:25 收工）

### 已完成

| 项 | 状态 |
|---|---|
| 转 PCB + 飞线验证（U1↔U3 四根） | ✅ |
| 器件数核对（补记 TP4=`EN` / TP5=`BOOT`） | ✅ |
| 临时板框 90×100 + 画布原点在左下角 | ✅ |
| 9 个轴座：坐标输入 → 翻底层 → 组合 → 锁定 | ✅ 位置、层向、焊盘网络(pin1=`KEY_xx`/pin2=`GND`) 均已核 |
| I2S 三线反序改映射 + 原理图改完 + DRC 0 error | ✅ |
| 四路独立核查（乐鑫 / MX 热插拔 / 嘉立创EDA / AMS1117） | 🔄 MX 那路已回（结论见 C1b）；其余三路结果未回，**下次开工先要** |

### 下一步（按顺序）

1. **先划元件带从左到右的分区**（C2d §5）—— 三项改动重排了元件带顺序，逐件摆容易返工。别忘给 U2 的 tab 预留连续 `+3V3` 铜面
2. 摆 U1（方案①：天线**在俯视投影上**完全出左板框，`Alt+M` 量悬出长度）
3. 摆 OLED 母座 + 4 孔（21×21 孔距刚性）
4. 摆 U3+C8/C9/C10 藏屏下、J5 移出屏投影、喇叭占位框放 U1↔OLED 间背面
5. 摆 Type-C（方案 A：上边缘中部偏右）+ U2+C1~C4（**C4 最贴 pin2，C3 次之**）+ 其余

### 尚未闭环的待办（别忘）

| # | 待办 | 位置 |
|---|---|---|
| 1 | **是否改用 `KEYBOARD-SW` 封装**（含 27 个孔，免手加）—— 需先在副本工程复核定位孔 Ø1.905 vs 官方 Ø1.702 | C1b |
| 2 | 键区 27 个机械孔（若不换封装）+ 手量 0.49mm 间隙 + 判定 DRC 是否覆盖挖槽区域 | 阶段 E |
| 3 | 下单前走 DFM 查 NPTH↔NPTH 孔距（官方 0.45mm 只覆盖有铜孔） | 阶段 I |
| 4 | 修正 `02-bom-footprints.md` 中 C41430893 的厂牌（瀚源，非 Kailh） | BOM |
| 5 | 另三路核查结果回收并回填 | — |
| 6 | 原有 12 项实物验证清单 | 第十一节 |
